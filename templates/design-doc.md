---
doc_type: DesignDoc
title: <対象> 設計書
status: draft | review | approved | implemented
owner: <設計者>
updated: <YYYY-MM-DD>
requirements: <要件定義書へのリンク>
adrs: []   # この設計から生まれたADRへのリンク
---

# Design Doc: <対象>

## 1. 文脈と目標

<!-- 要件定義書があるならリンク＋この設計が担う範囲だけ書く。二重記入しない
     （同じ内容を2箇所に書くと、片方だけ更新されて矛盾し、AIは古い方を信じることがある）。 -->

- 目標:
- 非目標: <!-- この設計で解かないこと。「将来必要そうだが今は設計しない」ものを明記 -->

## 2. 設計概要

<!-- 全体像を10行以内＋必要なら図（Mermaid可）。新規登場するコンポーネントと
     既存システムとの接続点が分かること。 -->

## 3. 詳細設計

<!-- 実装者(AI)が迷わない粒度で。特に以下はAIが独自判断しがちな点なので明示する:
     - データモデル/スキーマ（型・null許容・一意制約まで）
     - API/インターフェース（エラー時のレスポンス形式まで)
     - 状態遷移・並行性（同時に起きたらどうなるか）
     逆に、自明な部分・どちらでもよい部分は「実装者判断でよい」と明記して省く。
     全部を細かく書くとAIは思考停止して転写するだけになり、設計の穴に気づかなくなる。 -->

### 3.1 <コンポーネント/データモデル>

### 3.2 <API/インターフェース>

### 3.3 実装者判断でよい部分

- <例: 内部ヘルパーの分割粒度、ログメッセージの文言>

## 4. 既存コードとの関係

<!-- AI実装者への地図。従うべき既存パターンの実例と、触ってはいけない領域。
     これが無いとAIは独自流儀で書き、コードベースに方言が増える。 -->

- 従うパターン: <file:line — 「◯◯は この実装 に倣う」>
- 触らない領域: <ディレクトリ/ファイル>（理由: ）
- 影響を受ける既存機能: <壊れていないことをどう確認するか>

## 5. 検討した代替案

<!-- 各案2〜4行: 概要／採用しなかった理由。詳細な検討が必要な判断は ADR に切り出してリンク。
     棄却理由が無いと、AIは実装中に「こちらの方が良いのでは」と棄却済み案へ寄っていく。 -->

### 案A: <名前>（不採用）

- 概要:
- 不採用の理由:

## 6. テスト戦略

<!-- 何をどのレベル（unit/integration/E2E）で検証するか。
     「テストしないこと」も書く（AIはカバレッジのために無価値なテストを量産する）。 -->

- 検証する:
- テストしない: <例: 外部SaaSの挙動そのもの>（理由: ）

## 7. リリース・移行

<!-- 必要な場合のみ。データ移行・フィーチャーフラグ・ロールバック手順。不要なら削除。 -->
