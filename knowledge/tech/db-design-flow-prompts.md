---
type: Playbook
title: 要件→DB→処理→実装フローのプロンプト集（実行順・現行版のみ）
description: DB設計〜E2Eフローで実際に使っている全プロンプトの現行版全文を実行順に配置。経緯と原理は db-design-validation-from-requirements.md 側、こちらは実行用の写経元。
tags: [db-design, prompts, codex, pleasanter, playbook]
timestamp: 2026-07-24T00:00:00+09:00
---

経緯・原理・失敗の記録は[本編](/tech/db-design-validation-from-requirements.md)。
このファイルは**実行用の写経元**。フェーズ順（＝実行の時系列順）に並ぶ。
旧版は保存しない — 改訂したらここを上書きする。旧番号は（旧#n）で併記。

# 現在の実行キュー（2026-07-29 時点。片付いたら上書き）

**現在地: ローカル検証環境（Pleasanter＋MCP）でパッケージのインポート成功。
動作確認の途中。**

## いま打つ1本（全文。ここを毎回上書きする — 写経元はここ）

```markdown
インポート済みのローカル Pleasanter で動作確認を実行せよ。
「実装がない」という判断で停止することを禁ずる — まず実際に動かして
みよ。失敗したら、実際のエラー・画面の状態をそのまま報告せよ
（「無いはず」という推測でなく、観測された事実のみ）。
1. 拡張HTML の画面が表示されるか
2. 画面から操作して、保存が独自テーブルに1件入るか
3. job を1本実行して動くか
報告: 画面 <○|×（実際に何が出たか）> / 保存 <○|×（エラーの実物）> /
job <○|×（エラーの実物）>
```

その後の並び（1本ずつ、都度チャットで指定する）:
1. 動作確認3点 ← いまここ
2. ×が出たら: エラーの実物で分岐（ファイル無し系→ブランチ確定 #4c-0c 後半）
3. 全○になったら: #4c ローカル検証キャンペーン（妥協点仕分け・手順書
   リハーサル・acceptance TEST）
4. #5 構築の締め（台帳同期・仮決め回収・backlog 確定・引き渡しセット）

運用: 実行は常に「チャットで直近に指定された1本」だけ。ファイル内の
過去の続報プロンプト群（#4c-0a/0b 等）は履歴であり、指定されない限り
打たない。

---

# 旧キュー参考（2026-07-27 時点。構築モード移行で凍結）

前提の再確定（ユーザー訂正）: **meta-sync の空は被害ではなく未着手** —
メタ情報の実体設計はまだ存在しない。よって復旧・棚卸し・参照仕分けの
考古学は全廃し、**メタ層は作り直す**。上流（requirements / db-design /
process-design / scenario）は本日のスイープで中身ありの○を確認済みで健全。
古い断片との食い違いは再設計後の検証が検出する。

1. ~~git 管理化~~ 完了（台帳を git 管理下の 02_metadata/ へ移動）
2. ~~メタ層の再設計（3.14→3.14a）~~ 完了（論理5行＋同期 PROC 5本が台帳に）
3. **3.15 Pleasanter メタ情報層の土台設計 → 人間○×** ← いまここ
   （ユーザー指摘2連: スロット割当・同期統合の前に、サイト構成と
   「1アイテム＝業務の何か」の意味論が未設計だった。3.13 の項目設計は
   3.15 に吸収 — 残るのは検査のみ）
4. **3.14b 同期 PROC の統合**（土台確定後に1回だけ）
5. **3.12 サイトパッケージ生成**（site-spec から機械生成）
6. 並行可: Q-xx 回答提案の○× / 6.2 レビュー用 DB 設計書

運用: 実行は常に「チャットで直近に渡された1本」だけ。まとめ渡しはしない。

# 0. 共通規律（すべてのプロンプトの前提）

- **基線（コード・画面を読む走査のみ）**: mjs・HTML・サイトパッケージ等の
  コードを読む走査（3.1、T1〜T3）だけは基線上で行い、報告にコミットハッシュを
  含める。**台帳ファイルだけを読む作業に git の状態は関係ない**。
  危険ブランチはタグを打って削除（チェックより危険物の撤去）。
- 検証の「○」報告は**全行検分の表とセット**のときだけ信用する。
- 台帳はすべて凍結。変更は差分提案→人間承認→反映。人間の決定は
  requirements.md の「## 決定事項」に DEC-xx として記録し、そこから引用する。
- 報告は件数＋パターンの集約形式（手転記前提）。ID 列挙は業務PC内でのみ。
- 新しいプロンプトはチャットで渡すと同時にこのファイルへ追記する。
- **プロンプト本文内で他プロンプトの番号（5.1 等）を参照しない** — 受け取る側は
  この集を持っていない。本文は自己完結させる。番号は人間のナビ用のみ。

# フェーズ1: DB 設計

## 1.1 DB 設計生成 v6（旧#1）

新セッションに全文貼付。`<>` を埋める。典拠: 要件定義書（REQ-ID 済）/
items.md / existing-tables.md（alias 付き）。

```markdown
以下の要件定義書と典拠ファイルから DB 設計を作成し、サブエージェントで検証し、
実行検証まで行え。

## 典拠（設計はこの3つからのみ導出する。無い事実は推測せず「未確定」に積む）
- 要件定義書（末尾に貼付。既に REQ-ID が付与済み — その ID をそのまま使え。
  振り直し・欠番の詰め直しをしてはならない）
- items.md — 項目定義台帳。固定列の設計はこのファイルのみを典拠とする。
  entry: display の項目と「未確定」の項目は列にしない。
- existing-tables.md — 既存テーブルカタログ（alias = 業務上の呼び名付き）。
  既存テーブルは設計対象外・変更禁止・参照のみ。
  カタログに無いテーブルを参照してはならない。

## 事実と判断の境界
典拠に無い「事実」（型・必須・規則・既存テーブルの実在）は未確定に積め。
ただし「項目をどのテーブルに配置するか・新規テーブルを作るか・どう正規化するか」は
設計判断であり、あなたの仕事である。未確定に逃がすな。

## 規模の前提（この規模を超える設計は過剰である）
- 同時ユーザー数: <N> / データ行数: 最大テーブルで <N> 行/年
- 運用想定年数: <N> 年 / 開発・運用チーム: <N> 人

## フェーズA: 生成（あなた自身が行う）
1. 要件定義書を requirements.md として保存し、末尾に「## スコープ宣言」を書く:
   - 対象外: REQ-<ww, ...>
   - 時点保存必須: REQ-<xx, ...>
2. 既存テーブル対応表: existing-tables.md の全テーブルについて
   `テーブル名 / 満たしうる REQ-ID（なければ「該当なし」と明記）` の表を出力せよ。
   この表を作らずに設計を始めてはならない。対応づいた既存テーブルは
   新規テーブルでなく external 参照で満たすこと。
3. エンティティ台帳: エンティティと関連を列挙。evidence: [REQ-xx] 必須。
4. 物理設計: YAML 台帳で db-design.yaml に保存。散文・ER 図不要。
   evidence の書き方（全要素に必須）:
   - 業務要素: evidence: [REQ-xx]。列は item: ITEM-xx も併記
   - 構造要素（主キー・FK・親表）: evidence: {structural: <支える要素名>}
   - 既存テーブル参照: evidence: {external: <existing-tables.md 上の名前>}
   - 規則: 全要素は REQ 直接参照か structural 連鎖で REQ 持ち要素に到達すること。
     到達しない要素は speculative: true + reason 1行。
5. 全数配置規則: items.md の entry: input かつ型確定済みの全 ITEM を、必ず
   いずれかのテーブルの列として配置せよ（既存テーブルなら external で示す）。
   配置できない ITEM は理由付きで「配置不能」として報告せよ。

## 明示的な許可（禁止事項より優先する）
- REQ-<xx, ...> のため、外部定量値・Excel 計算結果・しきい値の時点保存
  （スナップショット）は必須である。禁止リストの対象外とする。
- 可変行・汎用列は items.md の項目定義に基づき固定列に正規化して表現する。
  EAV・JSON 列・汎用 extra/meta カラムを使わないこと。

## 禁止事項（上の許可に該当しない限り入れない。必要なら REQ-ID を挙げて反論せよ）
### 構造
- Read Model・CQRS 分離・集計・スナップショット・検索用非正規化・
  キャッシュテーブル。読み取りは基本テーブルへのクエリまたはビューで満たす。
  反論は性能要件（応答時間・行数）が REQ に明記されている場合のみ。
- FK を持たない独立テーブル（単独で REQ を満たす evidence がある場合を除く）。
### 列
- 技術的補助列: 表示順・非正規化カウント・導出可能な集計値・キャッシュ列・
  ステータス予備値・汎用 extra/meta カラム。導出できる値は保存しない。
- 履歴テーブル / 監査列 / ソフトデリート / マルチテナント / 汎用中間テーブル / 予約列。

## フェーズB: 検証（サブエージェント2体を並列起動）
渡すのは requirements.md・db-design.yaml・items.md・existing-tables.md の
パスのみ。会話の経緯・設計意図は伝えない。両者とも編集禁止・報告のみ・再委譲禁止。
- 削除審問: 各テーブル・列の削除を試みよ。削除で満たせなくなる REQ-ID を挙げよ。
  REQ-ID がない要素は、削除すると evidence 持ち要素が機能しなくなる構造的依存
  （主キー・FK・親子）を挙げよ。structural は参照先の実在と evidence を、
  external は existing-tables.md 上の実在を検証し、実在確認できた要素は
  keep と判定せよ。削除提案の対象は、どの語彙でも根拠を示せない要素のみである。
  設計を正当化するな。
  報告: `対象 / keep|delete / 根拠(REQ|structural|external) または削除理由` の表のみ。
- 順トレース: スコープ宣言で対象外の REQ-ID は「対象外」と報告して除外せよ。
  残りの各 REQ-ID を満たすテーブル・列が存在するか判定せよ。既存テーブルで
  満たすものは covered(external)。「おそらく満たせる」は不足として扱え。
  missing と判定する前に existing-tables.md の全テーブルを確認し、
  不足内容に「既存テーブルで満たせない理由」を1行で必ず書け。
  この理由が書けない missing 判定は無効である。
  さらに items.md の各 ITEM（entry: input・型確定済み）が db-design.yaml に
  item: ITEM-xx として現れるか判定し、現れないものを missing(item) と報告せよ。
  報告: `REQ-ID|ITEM-ID / covered|covered(external)|missing|対象外 / 不足内容` の表のみ。

## フェーズC: 統合（あなた自身が行う）
- 削除提案: 要件書・items.md・existing-tables.md の記載のみを根拠に反論できるなら
  keep、できないなら削除。生成時の意図を根拠にしない。
- 不足: 設計に追加（evidence 必須）。
- 修正後の db-design.yaml に順トレースをもう一度だけ起動。
  missing が 1 件でも残ればフェーズDへ進まず停止し、報告のみ出せ。

## フェーズD: 実行検証（missing 0 の場合のみ）
1. db-design.yaml から DDL を生成し SQLite で実行。既存テーブルは
   existing-tables.md の key・主要列だけのスタブとして作成してよい。
   エラーは握りつぶさず報告。
2. 各画面・帳票・バッチの代表シナリオ（INSERT→SELECT の列）を SQL で書き実行。
   SQL は scenario/*.sql として保存して残せ。
3. どのシナリオにも触られないテーブル・列を報告せよ（過剰の物証）。
4. シナリオを書こうとして書けなかった要件を報告せよ（不足の物証）。

## 最終報告（この形式のみ。ID の列挙は不要、件数とパターンで書く）
- 削除提案: <件数> / 反論keep: <件数> / 内訳上位パターン: <2〜3語>
- 順トレース再実行: missing <件数>（あればパターンと理由の要約）/
  missing(item) <件数> / 対象外 <件数>
- フェーズD: DDL <成功|失敗> / シナリオ <実行数>/<要件数> /
  未使用 <件数> / 書けなかった要件 <件数>（あればパターン）
- 残差: speculative <件数> / 反論keep <件数> / 未確定 <件数>
- 判定: (a)誤検知のみ (b)missing 0 (c)全シナリオ実行可 を ○× で

## 個別指示（規則の所在）
<「規則がない」と判定された REQ の規則の所在を1行ずつ書く>
```

## 1.2 確定モード（旧#2）

数字が振動し始めたら測定モード（新セッション再生成）を終了しこちらへ。

```markdown
## 確定モード（再生成禁止）
db-design.yaml は現行の確定候補である。ゼロから設計し直してはならない。
変更は以下の解消作業に限る。
1. missing 各件: 典拠から解消案を作り最小差分で反映せよ。典拠から解消できない件は
   「質問: <ID> / 提案: <案> / 必要な決定: <1行>」の形で人間向けリストに出せ。
2. 未確定 各件: どの典拠を確認しなぜ答えが無かったかを1行で書け。典拠に答えが
   ある場合はその場で解消し確認箇所を記せ（items.md で確定済みの型・required を
   未確定扱いしていないか全件照合せよ）。残る件は「提案+根拠+確度」の○×リストへ。
3. 反映後、順トレースと削除審問を1回ずつ起動し、差分のみ報告せよ。
報告: 解消済み <件数> / 人間○×待ち <件数> / missing 残 <件数>
```

## 1.3 フェーズD報告の統合指示（旧#3）

```markdown
フェーズDの報告を以下の規則で処理せよ。db-design.yaml は凍結、最小差分のみ。
1. DDL エラー: yaml 側を修正して DDL を再生成・再実行せよ。
   制約（NOT NULL・FK）を外してエラーを回避することを禁ずる。
2. 未使用テーブル・列: evidence を確認し、
   - REQ 直接参照あり → その REQ の代表シナリオを追加して再判定
   - speculative / structural のみ → 削除候補として人間向けリストへ
3. 書けなかった要件: 理由で分類せよ。
   - 列・テーブル不足 → 「DB 差分提案」（勝手に反映するな）
   - 関係が表現できない → 対象と理由を個別報告（構造欠陥の疑い）
   - 規則が曖昧 → 「未確定: 何が決まれば書けるか」を人間向けリストへ
4. 処理後フェーズDを再実行し、差分のみ報告せよ。
報告: エラー解消 / シナリオ追加 / 削除候補 / DB 差分提案 / 未確定 / 実行 <成功数>/<総数>
```

# フェーズ2: 処理設計

## 2.1 処理設計（旧#4）

DB 確定後。db-design.yaml は凍結入力。

```markdown
以下の典拠から処理設計（画面×DB の CRUD 対応と規則仕様）を作成し、
サブエージェントで検証し、実行検証まで行え。

## 典拠（この5つのみ。無い事実は推測せず「未確定」に積む）
- requirements.md（REQ-ID 付与済み。振り直し禁止。スコープ宣言込み）
- db-design.yaml — 確定済み DB 設計。変更禁止（凍結）。
- existing-tables.md / items.md / 画面 HTML ファイル群

## 凍結と差分提案
db-design.yaml は変更してはならない。列・テーブルの不足に気づいた場合は
「DB 設計への差分提案」として `対象 / 提案 / 根拠 REQ-ID` のリストに出せ。

## フェーズA: 生成
1. 処理の列挙: 画面 HTML と要件から処理一覧を作れ。PROC-01 形式の ID、
   種別（画面操作 / 帳票 / バッチ）、evidence: [REQ-xx]。
   どの処理にも紐づかない画面、どの画面にも紐づかない REQ を明記せよ。
2. 処理台帳: process-design.yaml に保存。各処理:
   - process: PROC-xx
     name: <処理名>
     trigger: <画面名・操作 | バッチ起動条件 | 帳票出力>
     reads: [テーブル.列, ...]   # db-design.yaml か existing-tables.md に実在のみ
     writes: [テーブル.列, ...]
     rule: <計算・判定規則。要件原文の引用必須。引用できなければ「未確定」>
     impl: <pleasanter標準 | 独自API/job | バッチ | 未定>
     evidence: [REQ-xx]
3. 規則の規律: rule に書けるのは要件原文から引用できる規則のみ。
   引用できない規則は「未確定: <何が決まっていないか>」。

## フェーズB: 検証（サブエージェント2体、requirements.md・db-design.yaml・
process-design.yaml・existing-tables.md のパスのみ。編集禁止・報告のみ・再委譲禁止）
- カバレッジ検証: スコープ宣言の対象外を除く各 REQ-ID がいずれかの process で
  実現されるか。「おそらく」は不足。missing には理由1行必須。
  報告: `REQ-ID / covered|missing|対象外 / 理由` の表のみ。
  ※対象外にできるのはスコープ宣言に列挙された REQ のみ。自前判断の対象外は不可。
- 使用審問: db-design.yaml の全テーブル・全列が reads/writes に現れるか。
  現れない列は「未使用」。process の reads/writes の実在も検証し、
  実在しない参照を「幽霊参照」として列挙せよ。
  報告: `対象 / used|未使用|幽霊参照 / 備考` の表のみ。

## フェーズC: 統合
- missing: 処理追加か規則補充（evidence 必須）。書けない件は
  「質問 / 提案 / 必要な決定」の○×リストへ。
- 未使用列: 書き漏れなら処理側を直す。本当に不使用なら「DB 差分提案（削除）」へ。
  幽霊参照は必ず解消。解消後、検証2体をもう一度だけ起動し差分のみ報告。

## フェーズD: 実行検証（missing・幽霊参照 0 の場合のみ）
1. DDL を SQLite で実行（既存テーブルはスタブ可）。
2. 各 process の rule を反映した SQL を書き実行。scenario/PROC-xx.sql に保存。
3. SQL にできなかった process と理由を報告。

## 最終報告（件数とパターンのみ）
- 処理数（種別内訳）/ impl 内訳（pleasanter標準 / 独自 / バッチ / 未定）
- カバレッジ: missing / 対象外 ・ 使用審問: 未使用列 / 幽霊参照
- フェーズD: DDL / SQL 実行 <成功数>/<処理数> / 書けなかった処理
- 残差: 未確定 rule / 人間○×待ち / DB 差分提案
- 判定: (a)missing 0 (b)幽霊参照 0 (c)全処理の SQL 実行可 を ○×
```

# フェーズ3: Pleasanter 連携（constraints・同期台帳・store・impl）

## 3.1 サイトパッケージ・mjs からの抽出（旧#5）

```markdown
以下を順に実行せよ。
1. pleasanter-constraints.md の生成: 既存サイトパッケージ JSON を読み、
   項目種別・スロット・選択肢定義・設定値を抽出して台帳化せよ。
   パッケージに現れない機能・項目は「未確認」と明記せよ。
2. 現行マッピングの抽出: プロジェクトの mjs を読み、コードが行っている
   項目↔データの対応をすべて抽出し meta-sync.yaml の下書きとして出力せよ。
   - 各行に evidence: {code: <ファイル名:関数名>, baseline: <基線タグ>} を必ず付けよ。
   - コードの対応は「現状の事実」であり「正しい設計」ではない。補正せずそのまま。
   - direction / source / sync はコードから断定できる場合のみ根拠行を引用して書き、
     断定できなければ「未確定」。推測で埋めるな。
3. 範囲フィルタ: 抽出した対応のうち、field が pleasanter-constraints.md の
   標準項目に実在する行のみを meta-sync.yaml とせよ。それ以外
   （拡張HTML と独自テーブル・既存DBの結線）は ui-binding.yaml に分けよ
   （同期台帳の対象外だが、mjs↔設計台帳の突合用に保持する）。
4. 突合報告: 抽出した対応のうち db-design.yaml・existing-tables.md のどちらにも
   現れない項目を「台帳外の対応」として列挙せよ。
報告: constraints 抽出項目数 / meta-sync 行数 / ui-binding 行数（確定/未確定）/
台帳外の対応 <件数>
```

※範囲フィルタを忘れると全結線が meta-sync に流れ込み幽霊が数百件出る
（実測: 幽霊277）。

## 3.2 source 未確定の一括トリアージ（旧#6）

判別質問: 「導入後の世界で、この値を最初に作り・更新し続けるのは誰か」
（既存システム→既存DB / 画面ユーザー→pleasanter直接 / 新アプリ処理→新規）

```markdown
meta-sync.yaml の source 未確定の各行について、次の3観測に基づき提案せよ:
1. mjs のコード: どちらから読みどちらへ書いているか（読み元=正の候補。引用）
2. process-design.yaml: この値を writes する process があるか
3. existing-tables.md: usage に更新主体の記載があるか
提案形式: `field / 提案 source / 根拠（観測1〜3+引用）/ 確度`
提案できない行は「質問: <field> は導入後、誰が最初に入力・更新するか？」の
質問リストにせよ。
```

## 3.3 メタ情報同期台帳の検証（旧#7）

```markdown
meta-sync.yaml に対して検証を実行せよ。サブエージェント1体、
ファイルパスのみ渡す。編集禁止・報告のみ。
- 実在: source が existing-tables.md / db-design.yaml に、field が
  pleasanter-constraints.md に実在するか（幽霊検出）
- 単一の正: 同じ事実に source が2つ以上ある行、direction が矛盾する行
- sync 整合: sync の process が process-design.yaml に実在するか。
  無ければ「同期処理の新設提案」として列挙
- コード突合: 各行の決定と mjs の実際の読み書き方向の矛盾を列挙（=mjs 改修箇所）
報告: 幽霊 / 二重の正 / sync 未整備 / mjs 矛盾 の件数
判定: (a)幽霊0 (b)二重の正0 を ○×
```

## 3.4 幽霊 source の実在確認（3分岐）（旧#7a）

```markdown
幽霊とされた source の各件について、実DB（既存スキーマ）で実在を確認し3分類せよ:
- 実在する → existing-tables.md に追記（table / key / 該当列 / usage / alias）して解消
- 別名で実在（表記ゆれ）→ existing-tables.md の alias に追記して解消
- 実在しない → 「mjs が参照している死んだ列」として mjs 改修箇所リストへ
報告: 追記 <件数> / 表記ゆれ <件数> / 死亡参照 <件数>
```

## 3.5 死亡参照の仕分け（デッドコード or 別の出所）（旧#7c）

```markdown
死亡参照の各件について、その値が現行アプリで実際に使われているかを
mjs の呼び出し経路から判定せよ:
- 呼び出し経路が無い（デッドコード）→ 「削除候補」
- 現に使われている → 実在しない列から値は来ない。実際の出所
  （API 計算値・ビュー・別テーブル・画面内計算）をコードを遡って特定し報告せよ。
  特定できなければ「出所不明」と書け。推測で埋めるな。
報告: 削除候補 <件数> / 出所判明 <件数>（出所の種類別）/ 出所不明 <件数>
```

出所が DB 列でないもの（API 計算値等）は meta-sync の source 語彙を拡張して
載せる（例: `source: 計算値（出所: <関数>）` — 同期不要・表示のみの行として区別）。

## 3.6 同期処理の差分追加（旧#7b）

sync 未整備が実処理 N 件に集約されたとき。凍結中の process-design.yaml への
人間承認済み差分として通す。起動条件（イベントか定期か）は先に提案を
出させて人間が○×する。

```markdown
同期処理 <N> 件を process-design.yaml に追加せよ:
- PROC-xx 形式で採番 / trigger: <起動条件> / impl: バッチ
- reads / writes は meta-sync.yaml の該当行から導出
- evidence: 対応する meta-sync 行と REQ-ID
追加後、meta-sync.yaml の該当行の sync 欄に新 PROC-ID を記入し、
同期台帳の検証（source・field の実在 / 単一の正 / sync の process 実在 /
mjs との読み書き方向突合）を再実行し、全行検分の表とともに報告せよ。
```

## 3.7 store 一括付与（機械導出）（旧#8）

```markdown
db-design.yaml に store を一括付与せよ。
1. 既定: 全テーブル・全列に store: 独自 を付けよ。
2. 冒頭に宣言: 「業務データは独自テーブルに保存する。Pleasanter 標準項目への
   格納は meta-sync.yaml に列挙されたメタ情報のみ」
3. 例外検査（提案のみ。勝手に変更するな）:
   a. meta-sync.yaml の source が pleasanter直接/既存テーブルの field と同内容の
      列を保持していないか → 二重保存疑い（削除して参照/同期に置換の提案）
   b. existing-tables.md の既存列と同内容の列 → 同上
   例外: 許可節の時点保存（スナップショット）列は対象外。
報告: 付与列数 / 二重保存疑い <件数>（`列 / 重複相手 / 提案` の一覧）
```

## 3.8 impl 仕分け（旧#9）

```markdown
process-design.yaml の impl を全件確定させよ。
値は pleasanter標準 | 独自（拡張HTML/mjs/API）| バッチ の3値。
典拠: process-design.yaml / pleasanter-constraints.md /
db-design.yaml（store 宣言済み）/ meta-sync.yaml

判定規則（上から順に適用し、各件に規則番号と根拠を書け）:
1. reads/writes に store: 独自 の列を含む → pleasanter標準 単独不可。独自かバッチ。
2. trigger がバッチ起動・定期実行、または meta-sync の同期新設分 → バッチ。
3. pleasanter標準 は「触るデータが標準項目（と既存DB参照）のみ、かつ機能が
   constraints.md に実在」の場合のみ。
4. 標準でできそうだが constraints.md に記載なし → 「要実機確認」として別掲。
5. 決まらない件は「質問: <何が決まれば確定するか>」。未定のまま残すな。

報告: impl 内訳（4分類+質問）/ 独自の一覧（PROC-ID+1行 = 実装バックログ）/
要実機確認の一覧 / 判定: 質問0 で完了
```

## 3.9 メタ同期層の存在理由審問（旧#9a）

impl 仕分けで pleasanter標準 が 0 件だった場合に実行。

```markdown
meta-sync.yaml の各行の evidence に挙がる REQ について、requirements.md の
原文を引用し、次を判定せよ:
- その REQ は Pleasanter 標準機能（一覧・絞込・通知・検索・権限等）が
  満たしているか（処理台帳に載らない標準動作としての充足）
- それとも独自処理が満たしているか
報告:
- covered(標準機能) の REQ 一覧 — 1件でもあればメタ同期層の存在理由は健在
- 全 REQ が独自処理で満たされている場合:
  「メタ情報の Pleasanter 掲載をやめたときに満たせなくなる REQ」を挙げよ。
  挙げられなければ、メタ同期層（台帳+同期ジョブ）の削除を提案せよ。
  正当化するな。
```

## 3.10 メタ同期行の刈り込み（旧#9b）

```markdown
meta-sync.yaml の各行について、その field が「標準機能で満たされる REQ」の
実現（一覧・通知等の対象項目）に使われるか判定せよ。
- 使われる → keep（根拠 REQ を付記）
- 使われない → 削減提案（この行の同期をやめても当該 REQ は満たせるはず。
  やめると壊れる別の REQ があれば挙げよ）
報告: keep <行数> / 削減提案 <行数>
```

## 3.11 サイト構築成果物の実在監査（「実装が API と job しかない」の点検）

バックログは process-design.yaml の impl: 独自|バッチ の PROC からしか生成
されないため、**サイト構築（PROC でないインフラ作業）はバックログに載る経路が
無い**。メタ情報・拡張HTML の着地先サイトが成果物として実在するかは、
バックログでなくこの監査で確認する。

```markdown
Pleasanter サイト構築の成果物が存在するかを監査せよ。
読むだけでよい — 判断・修正・生成をするな。

## 前提の宣言
本プロジェクトは拡張HTML＋独自テーブル構成であり、業務データは独自テーブルに
着地する。しかし meta-sync.yaml のメタ情報と拡張HTML は、Pleasanter サイトが
構築されて初めて着地先を持つ。サイト構築の成果物が無ければ、実装物
（API・ジョブ）は宙に浮く。

1. 成果物の棚卸し: 以下の各項目について 存在（パス付き）/ 不在 の表を出せ:
   - サイト構築仕様（サイト設計の台帳。mapping.yaml 等の名前を問わず探せ）
   - インポート可能なサイトパッケージ JSON（新規構築用）
   - pleasanter-constraints.md（項目種別・スロットの制約台帳）
   - 抽出元にした既存サイトパッケージ JSON
2. meta-sync.yaml の全行について、その field を受けるサイト・項目の定義が
   1の成果物のどれかに存在するかを全行検分し、
   `field / 定義あり(成果物名) | 未定義` の表で報告せよ。
3. 拡張HTML の各画面について、どのサイトに配置するかの定義がどこにあるかを
   示せ。定義が無ければ「配置先未定義」と報告せよ。
4. backlog.md（引き渡し成果物の節を含む）に「サイト構築・サイトインポート」に
   相当する行があるか判定せよ。無ければ「バックログ外の作業」と報告せよ。
5. process-design.yaml の impl 内訳を件数で報告せよ
   （pleasanter標準 / 独自 / バッチ）。

報告: 棚卸し表 / meta-sync 未定義 <件数> / 配置先未定義の画面 <件数> /
backlog 該当行 <有|無> / impl 内訳
判定: サイト構築が「成果物あり」「バックログか引き渡し成果物に行あり」の
どちらかで捕捉されているか ○×
```

不在が確定した場合の次の一手: サイト構築仕様（メタ項目の割当＋拡張HTML の
配置先）を台帳として新設し、可能ならサイトパッケージ JSON を機械生成して
「サイト構築＝台帳からのインポート」にする（台帳＝実機の一致を構造的に保証）。
インポート形式は実環境バージョンで要確認。→ 生成は 3.12。

## 3.12 サイト構築仕様の台帳化とサイトパッケージ生成

3.11 で不在が確定したときの実行プロンプト。方針3点: ①形式の典拠は既存
サイトパッケージ JSON のみ（構造・キーの発明禁止 — インポート非互換の最大
リスク対策） ②JSON を直接書かせず site-spec.yaml（台帳）→機械生成の2段
（台帳＝実機の一致を構造的に保証） ③つなぎ込みは列挙して backlog 差分に
落とすまで — 実装は1件1セッションの実装ヘッダで別途（生成と実装を混ぜない）。

```markdown
Pleasanter サイト構築仕様を台帳として作成し、サイトパッケージ JSON を
機械生成せよ。

## 典拠（この集合のみ。無い事実は推測せず「未確定」に積む）
- meta-sync.yaml — Pleasanter 標準項目に載せるメタ情報
- pleasanter-constraints.md — 項目種別・スロット・設定値の制約。
  ここに現れない機能は「未確認」として扱え。
- 既存サイトパッケージ JSON — パッケージ形式の唯一の典拠。出力 JSON の
  構造・キーはこのファイルに実在する形のみ使え。形式を発明するな。
- process-design.yaml — impl: 独自|バッチ の PROC（API・ジョブ）の一覧
- ui-binding.yaml と 拡張HTML・mjs — 画面と結線の現状
- db-design.yaml / existing-tables.md — 参照のみ（変更禁止）

## フェーズA: サイト構築仕様 site-spec.yaml の生成
1. サイト構成: 必要なサイトを列挙せよ（サイト名 / 目的 / evidence）。
   拡張HTML をホストするサイトとメタ情報を保持するサイトを明示せよ。
2. 項目割当: meta-sync.yaml の全行について、着地するサイト・標準項目
   （スロット名）・表示名・選択肢定義を割り当てよ。項目種別は
   pleasanter-constraints.md に実在するもののみ。
3. 拡張HTML の配置: 各ローカル HTML / mjs / css を、サイトのどの拡張枠
   （スクリプト・スタイル・拡張HTML欄）に載せるかを割り当てよ。
   枠は既存パッケージ JSON に実在するもののみ。実在しない枠が必要なら
   「要実機確認」として別掲せよ。
4. API・ジョブの接続点: process-design.yaml の impl: 独自 の各 PROC について、
   拡張HTML からの呼び出し結線（画面イベント → API エンドポイント）を
   一覧化し、mjs の現状と突合して 結線済み / つなぎ込み残 を判定せよ。
   実装はするな — つなぎ込み残は backlog.md への差分提案として出せ。

## フェーズB: 検証（サブエージェント1体。ファイルパスのみ渡す。
編集禁止・報告のみ・再委譲禁止）
- 全数: meta-sync.yaml の全行が site-spec.yaml に着地するか（未着地を列挙）
- 実在: site-spec の項目種別・拡張枠が constraints.md / 既存パッケージ JSON に
  実在するか（幽霊検出）
- 結線: フェーズA-4 の一覧と ui-binding.yaml の突合（欠落・幽霊）

## フェーズC: パッケージ生成（フェーズBの指摘を解消してから）
site-spec.yaml からサイトパッケージ JSON を生成せよ。
- 構造・キーは既存パッケージ JSON に実在する形のみ。
- 自己検分: site-spec の全行（サイト・項目・拡張枠）がパッケージに現れるか
  全行検分の表で示せ。パッケージにのみ現れる要素は「発明」として列挙し
  削除せよ。
- JSON の構文検証を実行せよ（パースが通ること）。

## フェーズD: 要実機確認リスト
ローカルで白黒がつかない事項を1枚に列挙せよ: インポートの成否 /
バージョン差異による形式非互換 / 拡張枠の動作 / API 呼び出しの疎通。
各行に「実機で何をすれば白黒つくか」を1行で付けよ。

報告: サイト <数> / 項目割当 <行数>（meta-sync 未着地 <n>）/
拡張HTML 配置 <n>（要実機確認 <n>）/ つなぎ込み残 <n>（backlog 差分提案）/
パッケージ自己検分 <一致 | 発明 n 件> / 要実機確認 <n 件>
判定: (a) meta-sync 全行着地 (b) 幽霊 0 (c) パッケージ＝仕様の全行一致 を ○×
```

## 3.13 メタ項目ミニマッピングの提案生成（3.12 A-2 が詰まったとき）

実測: 3.12 の項目割当で「メタ項目の mapping ができていない」— 割当の決定が
どの成果物にも物質化されていなかった（サイトパッケージと同じクラスの穴）。
割当は事実でなく**設計判断**なので提案駆動 — モデルが根拠付き提案、人間が○×。

```markdown
meta-sync.yaml の全行について、Pleasanter 標準項目への割当
（ミニマッピング）を提案せよ。割当は設計判断であり、人間の○×を経てから
site-spec.yaml に反映する。勝手に確定するな。

## 典拠
- meta-sync.yaml — 対象の全行。これ以外の項目を割当に含めるな。
- pleasanter-constraints.md — 使える項目種別・スロット・上限はここに
  実在するもののみ。実在しない種別を提案するな。
- db-design.yaml / existing-tables.md — 型・値域の確認用（参照のみ）

## 各行の提案形式
- field / 提案スロット（サイト名.標準項目名）
- type_conv（型変換。危険があれば danger を1行 — 桁溢れ・選択肢上限・
  日付精度等）
- writer（誰がこの項目を書くか。meta-sync の source・direction と
  整合させよ — 矛盾する writer を提案するな）
- 根拠（なぜこのスロットか: 型適合と、標準機能（一覧・絞込・通知）が
  この項目種別で効くか）
- 確度(高|中|低)

## link（結合キー）の提案
独自テーブル行と Pleasanter アイテム（ItemId）の結合キーをどう持つかを
提案せよ。meta-sync・db-design に既定義があれば引用して示し、無ければ
提案＋確度で出せ（これが未定だと同期は必ずズレる — 空欄で流すな）。

## 検査（提案と同時に）
- 衝突: 同一スロットに2つ以上の field を割り当てていないか
- 上限: 使用スロット数が constraints.md の上限内か
- 割当できない field は「割当不能: 理由1行」として別掲せよ（ひねり出すな）

報告: 割当提案 <行数>（確度内訳）/ danger あり <n> / link <既定義引用 | 提案> /
割当不能 <n> / 衝突・上限超過 <有|無>
○×後: 承認された割当を site-spec.yaml の項目割当節に反映し、
サイトパッケージ生成へ進め。
```

**「対象0件」が返ったとき（実測: 割当対象0件・constraints の30項目や
過去抽出16件を勝手に追加できないと報告、何も生成されず）**: 0件報告は
全行検分の証拠とセットでないと信用しない（削除提案0件と同じ規律）。
仮説: (a) 暗黙フィルタで全行除外（field が既に標準項目名=割当済み解釈 —
なら真の未決はサイト帰属） (b) 行が本当に消えている（直近作業の巻き添え。
行が無ければ同期層検証は空虚に○が出る） (c) 参照ファイルの取り違え。

```markdown
ミニマッピングの対象が0件になった理由を、事実だけで報告せよ。
直すな・作るな。
1. meta-sync.yaml の現物: 全行を `field / source / direction / sync` で
   一覧にせよ（行数も報告）。空・行なしならそのまま報告せよ。
2. 0件判定の規則: 各行について「この行を対象外とした理由」を1行で
   対応付けよ。全行一覧と除外理由の対応が付かない0件報告は無効である。
3. 30項目・16件の正体: 追加候補に挙げた「constraints の30項目」
   「過去抽出の16件」がそれぞれ何のファイル・どのリストか、パスと性質を
   示せ。
4. 履歴: meta-sync.yaml の更新日時と、今回の復旧・Q化作業で変更を
   加えたか（加えたなら差分）を報告せよ。
報告: meta-sync 行数 <n> / 行ごとの除外理由 / 30項目・16件の正体 / 直近変更
```

読み方: 行があって除外理由が「field が既に標準項目名」なら、未決なのは
割当でなくサイト帰属・型変換・writer — プロンプトの問いを差し替える。
行が無ければ台帳復旧（T4）へ。取り違えなら正しいパスを名指しして再実行。

**実測の帰結（2026-07-27）: mapping が空配列 = 行の消失**（確定時点は6行。
status: draft は空骨格の名残と推定）。副発見: 行0なら同期層検証は何も検分せず
○が出る — **空虚な○**。スイープの各層報告に検分件数を必須化（下記パッチ）。

```markdown
meta-sync.yaml の mapping が空配列である。最終確定時点では6行あった。
行の復旧を以下の順で行え。
1. 消失の説明: meta-sync.yaml の更新日時とバックアップ（.bak・ごみ箱・
   一時ファイル）を確認し、いつ・どの作業で空になったかの説明を試みよ。
   実体が見つかればそれを復元し、手順3へ進め。
2. 逆再構成: 以下の素材から mapping 行を再構成せよ:
   - docs/design/30-integration.md（同期対応表 — meta-sync の機械生成
     ビュー。あればこれが最良）
   - process-design.yaml の同期 process（sync ジョブの reads/writes は
     meta-sync 行から導出されている — 逆向きに行を復元できる）
   - ui-binding.yaml・抽出台帳（範囲フィルタで meta-sync 側に残した行の
     手がかり）
   - 判断カードの結線記録（A 選択の反映分）
   各行に再構成元を注記せよ。field / source / direction / sync の欄が
   埋まらない場合は空欄にせず「未確定(要確認)」と書け。
3. 検証: 再構成後、同期台帳の検証（source・field の実在 / 単一の正 /
   sync の process 実在）を実行し、全行検分の表とともに報告せよ。
   行数 <n> を必ず報告せよ — 行数0での○は無効である。
4. 先の Q化（status: draft 由来の Q-xx）が指していた内容がどの行に
   対応するかを特定し、該当行に 未確定(Q-xx) を記せ。特定できなければ
   Q-xx の内容と再構成行の対応候補を報告して人間判断を仰げ。
報告: 消失の説明 <判明|不明> / 復旧経路（バックアップ | 逆再構成）/
復旧行数 <n>/6 / 検証（全行検分・行数付き）/ Q-xx 対応 <特定|要判断>
```

**続報3: 「ないものにマッピングされてる」の仕分け（逆再構成の前に実行）**。
ユーザー指摘で前提が確定 — メタ情報は論理設計（6行）済み・実体設計
（Pleasanter のサイト・項目種別）は未着手。よって**物理項目の断定参照は
すべて時期尚早の幽霊**であり、逆再構成で正本に焼き込む前に除去する。
論理名での参照は正常な中間状態（実体化は site-spec 設計の仕事）。

```markdown
メタ項目への参照を全数仕分けせよ。前提: Pleasanter 側のメタ項目の実体
（どのサイト・どの項目種別・選択肢）はまだ設計されていない。したがって
具体スロット名を確定値として書く参照はすべて時期尚早の幽霊である。
論理名（メタ情報の業務的な呼び名）での参照は正常であり触るな。
1. 参照の収集: meta-sync.yaml（骨格の field 名含む）/ process-design.yaml の
   同期 process の reads・writes / ui-binding.yaml / scenario/*.sql /
   docs/design/30-integration.md から、Pleasanter 側項目への参照を全数
   列挙せよ。
2. 各参照を2分類せよ:
   - 論理名: 業務的な呼び名での参照。正常 — そのまま。
   - 物理断定: Pleasanter の具体項目（Status・ClassA 等のスロット名）を
     確定値として参照しているもの。幽霊 — どの成果物のどの作業で
     書き込まれたか、遡れる範囲で由来を報告せよ。
3. 幽霊の処置は提案のみ（勝手に書き換えるな）:
   `箇所 / 現状の物理断定 / 論理名への書き換え案` の表で出せ。
報告: 参照総数 <n> / 論理名 <n> / 物理断定（幽霊）<n>（由来の内訳）/
書き換え案 <n>
```

承認後: 書き換えを反映してから meta-sync の論理6行の復旧（続報2）へ進む。
物理割当はミニマッピング（3.13 本体）で新規に設計する — 復旧と設計を
混ぜない。

※続報2〜3の復旧・仕分けは**廃止**（ユーザー判断 2026-07-27: 空は被害でなく
未着手 → 考古学をやめて作り直す）。後続は 3.14 の新規設計を使う。

## 3.14 メタ同期台帳の新規設計（論理のみ・作り直し版）

復旧でなくゼロから設計する版。物理スロット名の使用は引き続き禁止
（実体設計は 3.13→3.12 の後工程）。掲載理由の審問（3.9 の理屈）を
生成の前段に置き、「載せる理由のない行を作らない」を構造で保証する。

```markdown
メタ情報同期台帳 meta-sync.yaml を新規に設計せよ。論理設計のみ —
Pleasanter の具体スロット名（Status・ClassA 等）を書くことを禁ずる。
実体への割当は後工程で行う。台帳ファイル群は 02_metadata/ 配下にある。

## 典拠
requirements.md（スコープ宣言・DEC 込み）/ db-design.yaml（凍結）/
process-design.yaml（凍結）/ existing-tables.md / 拡張HTML・mjs

## フェーズA: 掲載理由の審問（行の候補出し）
Pleasanter 標準機能（一覧・絞込・検索・通知・権限）で満たす REQ を列挙し、
各件で REQ 原文を引用せよ。その実現のために標準項目へ載せる必要がある
メタ情報のみを行の候補とせよ。標準機能で満たす REQ を1件も挙げられない
メタ情報は台帳に載せるな — 掲載理由のない行を作らない。

## フェーズB: 台帳生成
各行:
- field: 論理名（業務的な呼び名。物理スロット名禁止）
- source: 正の所在（既存DB | pleasanter直接 | 新規）。判別質問
  「導入後の世界で、この値を最初に作り・更新し続けるのは誰か」で決めよ。
  断定できなければ「未確定」— 推測で埋めるな。
- direction: 単一方向のみ。双方向が必要に見える行は設計せず
  「双方向疑い」として人間判断リストへ出せ。
- sync: 写す process。process-design.yaml に実在する PROC のみ。
  無ければ「同期処理の新設提案」として別掲せよ（勝手に追加するな）。
- evidence: [REQ-xx]（フェーズAで引用した REQ）

## フェーズC: 検証（サブエージェント1体。ファイルパスのみ渡す。
編集禁止・報告のみ・再委譲禁止）
- source の実在（existing-tables.md / db-design.yaml）
- 単一の正（同じ事実に source が2つ以上ないか・direction の矛盾）
- sync の process 実在
- 行数を必ず報告せよ — 行数0での○は無効である。

報告: 行数 <n> / 掲載理由 REQ <n 件>（原文引用付き）/ source 内訳 /
未確定 <n> / 双方向疑い <n> / 同期新設提案 <n> /
検証 <○|×>（全行検分・行数付き）
```

**実測（2026-07-27）: 行数0＋提案5件で正着**。「sync は実在 PROC のみ」×
「新設は提案のみ」の2制約で、全行が新設 PROC を要する初回生成では行を書く
手段が無い（設計上のデッドロック — モデルは正しく停止した）。解除は人間の
○×経由。教訓: **実在制約と新設禁止を同時に課すと初回生成は必ず0行になる —
初回は承認ループを1周挟む**。

## 3.14a 提案の○×と反映（初回デッドロックの解除）

```markdown
メタ同期台帳の提案5行を、承認打診の形に整えてから反映せよ。
1. ○×リストの提示: 5行それぞれを
   `# / field / source / direction / 新設 sync の trigger 提案
   （イベント契機 | 定期。根拠1行）/ evidence` の表で示し、私の○×を待て。
   勝手に反映を始めるな。
2. ○が付いた行のみ:
   a. process-design.yaml に同期 process を PROC-xx で追加せよ
      （trigger: 承認された起動条件 / impl: バッチ / reads・writes は
      該当行から導出 / evidence: meta-sync 該当行と REQ-ID）
   b. meta-sync.yaml に行を書き込み、sync 欄に新 PROC-ID を記入せよ
3. 未確定3件: questions.md に Q-xx を採番して登録し、該当箇所に
   未確定(Q-xx) と記せ（新しい状態語を発明するな）。
4. 反映後、同期台帳の検証（source 実在 / 単一の正 / sync の process 実在）を
   再実行し、全行検分の表と行数を報告せよ。
報告: 承認 <n>/5 / PROC 追加 <n> / 行数 <n> / Q 登録 <n> / 検証 <○|×>
```

**実測: 1行=1PROC でメタ項目ごとに同期ジョブが作られた** — 機械的写像の
過剰。同期処理の自然な単位は項目でなく**起動条件×方向×源泉**。統合規則:
①trigger・direction・source系統が同じ行は1本にまとめる ②source: 新規は
独立ジョブ不要の可能性（値を作る既存 PROC の writes に Pleasanter 書き込みを
足す方が単純・遅延ゼロ）③分けるのは起動条件が本当に違うときだけ。

## 3.14b 同期 PROC の統合提案（1行=1PROC の刈り込み）

```markdown
メタ情報同期の PROC 群を統合する提案を作れ。提案のみ — 人間の○×を経て
から反映せよ。
1. 現状の一覧: 同期 PROC 全件を
   `PROC-ID / 対象 field / trigger / direction / source` の表で示せ。
2. 統合の判定: trigger・direction・source 系統が同一の PROC 群を1本に
   まとめる案を作れ。統合後の各 PROC について
   `新 PROC / 吸収する旧 PROC / 対象 field（複数）/ trigger / 根拠1行`。
3. source: 新規 の行について: その値を作る既存 PROC（process-design.yaml の
   該当処理）を特定し、「独立ジョブでなく当該 PROC の writes に Pleasanter
   メタ書き込みを追加する」案と「独立ジョブのまま」案の両方を、利害1行ずつ
   付けて示せ。
4. 分けたままにすべき PROC があれば、起動条件の違いを根拠に挙げよ。
   根拠が言えない分割は統合案に含めよ。
○×後: 承認された統合を process-design.yaml と meta-sync.yaml の sync 欄に
反映し、同期台帳の検証（source 実在 / 単一の正 / sync の process 実在）を
再実行して全行検分の表と行数を報告せよ。backlog.md を再生成し、実装
アイテム数の増減を報告せよ。
報告: 統合案 <旧n本 → 新m本> / 既存 PROC への吸収案 <n> / 分割維持 <n>
（根拠付き）/ ○×後: 検証 <○|×> / backlog 増減
```

## 3.15 メタ情報の決定（候補洗い出し → 人間の絞り込み — 現行版）

改訂経緯: 抽象「土台設計」→却下（Pleasanter を調べてから）→ 全設定盛り→
却下（できてないのはメタ情報の設計だけ）→ 5行前提の項目設計 →
却下「**なんで5つのままなの？項目もまだ絞れてない**」。
確定した理解: meta-sync の5行はモデルの下書きにすぎず、**「何を載せるか」の
決定（＝メタ情報の設計の本体）は一度も行われていなかった**。載せる項目の
決定は業務判断 — モデルの仕事は候補の全数洗い出しと判断材料まで、絞るのは
人間。教訓3つ: ①対象プロダクトの設定語彙を調べてから書く ②足りない一点
だけを埋める ③**モデルが生成した台帳行を「決定済み」の顔で後工程に
渡さない — 決定の出所（人間か生成か）を常に区別する**。

### 3.15-1 候補の洗い出し（今ここ）

```markdown
Pleasanter に持たせるメタ情報を何にするか、候補の洗い出しから作り直せ。
現在の meta-sync.yaml の5行はモデルの下書きであり確定ではない —
前提にするな。台帳は 02_metadata/ 配下にある。

1. 候補の母集団を作れ。以下から「業務の管理に使う属性」を全数拾え:
   - requirements.md（状況・担当・期日など管理系の記述がある REQ）
   - items.md（entry: input の項目のうち管理属性の性格を持つもの）
   - 拡張HTML・mjs が表示・入力している管理情報
   - 既存 Excel・既存テーブルの管理列（existing-tables.md の usage）
   各候補: `候補名 / 出所（引用）/ 内容1行`
2. 各候補に判断材料を付けよ（決めるのは私だ）:
   - Pleasanter 標準画面でこれを使う場面があるか（一覧に出す / 絞り込む /
     通知する / 検索する — 具体的な場面を1行。無ければ「標準画面での
     用途見当たらず」と正直に書け）
   - 載せない場合どこで管理されるか（独自テーブル / 拡張HTML 内 / 管理不要）
3. 推奨を付けよ: 載せる(確度 高|中|低) | 載せない。理由1行。
   用途の無い候補に無理に載せる理由をひねり出すな。
報告: 候補 <n> 件の表のみ。散文不要。
私が○（載せる）×（載せない）を付けたら、○の分だけで meta-sync.yaml を
書き直し、旧5行と同期 PROC 5本は新しい決定に合わせて破棄・改訂せよ。
```

### 3.15-2 確定項目の項目設計（○×後）

```markdown
決定済みのメタ情報（meta-sync.yaml の全行）について、項目設計だけを
作成せよ。一覧・通知・権限・拡張HTML 配置・同期には触れるな。
各行1行: `field / Pleasanter 項目（ステータス | 管理者 | 担当者 | 分類A.. |
数値A.. | 日付A.. | チェックA.. 等）/ 表示名 / 選択肢一覧（値と出所。
選択式でなければ「自由入力」）/ 既定値 / 必須 / 型変換の注意（あれば1行）`
型・値域の根拠は db-design.yaml・existing-tables.md から引け。
決められない箇所は「未確定: 何が決まれば決められるか」1行（推測禁止）。
報告: 全行の表のみ。散文不要。
```

○×で確定したらメタ情報の設計は完了。以降必要に応じて: 同期 PROC 統合
（3.14b）/ サイトパッケージ生成（3.12。一覧・通知等の残り設定はそのとき
既定値ベースで最小に決める）/ つなぎ込み。

（参考・旧全部盛り版の設計対象カタログ: テーブル種別 / 項目の有効化・
表示名・選択肢・既定値・必須 / 一覧・フィルタ / 通知・リマインダー /
ステータス制御・アクセス制御 — 出典 https://pleasanter.org/manual/664450 。
サイトパッケージはこの設定一式の JSON。必要になった項目だけ個別に決める。）

```markdown
Pleasanter のメタ情報を保持するテーブルの設計書を作成せよ。設計は
Pleasanter の「テーブルの管理」で実際に設定する語彙・単位で書くこと。
提案駆動 — 各決定に根拠と確度を付け、私の○×を待て。
台帳は 02_metadata/ 配下にある。

## 典拠
meta-sync.yaml（載せるメタ情報 5行）/ requirements.md（掲載理由 REQ 原文）/
db-design.yaml / existing-tables.md / 拡張HTML・mjs /
pleasanter-constraints.md（既存環境で確認済みの項目種別・制約）

## 設計書の構成（テーブルの管理の設定単位で書く）
1. テーブル一覧: 作成するテーブル（サイト）ごとに
   `テーブル名 / 種別（記録テーブル | 期限付きテーブル）/ 用途1行 /
   1レコード＝業務の何か（db-design のどのテーブルの1行に対応するか）/
   独自テーブルとの紐付け方（どの項目に何のキーを持つか）`
2. 項目設定: テーブルごとに、使用する項目の表
   `項目（Title | Body | ステータス | 管理者 | 担当者 | 分類A.. | 数値A.. |
   日付A.. | 説明A.. | チェックA..）/ 表示名 / 選択肢一覧（値と出所。
   ステータスは状態値の一覧）/ 既定値 / 必須 / 対応する meta-sync の field`
   meta-sync の5 field が全て着地していることを確認せよ（未着地を報告）。
3. 一覧・フィルタ: 一覧画面に出す列と並び順 / フィルタで絞れる条件。
   掲載理由 REQ（一覧・絞込系）と対応付けよ。
4. 通知・リマインダー: 必要な通知の条件と宛先。掲載理由 REQ（通知系）と
   対応付けよ。不要なら「なし」と明記。
5. 拡張HTML・スクリプトの配置: 既存のローカル HTML / mjs / css を
   どのテーブルのどの画面（新規作成・編集・一覧）に配置するか。
6. ステータス制御・アクセス制御: 必要な範囲だけ。不要なら「既定のまま」。
7. レコードの作られ方: 誰が・いつレコードを作るか
   （画面から手動 | API・ジョブが自動作成 | 移行スクリプトが初期投入）。

## 規律
- 掲載理由 REQ の各件が 2〜4 のどこかの設定で満たされることを対応表で示せ。
- constraints.md に無い機能・種別を使う場合は「要実機確認」と別掲せよ。
- 決められない箇所は「未確定: 何が決まれば設計できるか」1行（推測禁止）。

報告: テーブル <n> / 項目 <n>（meta-sync 未着地 <n>）/
REQ 対応 <n>/<掲載理由数> / 要実機確認 <n> / 未確定 <n>
○×後: この設計書が site-spec（サイトパッケージ生成の仕様 兼 つなぎ込みの
仕様）になる。→ 3.14b 同期統合 → 3.12 パッケージ生成へ。
```

参考（設計対象の裏取り）: テーブルの管理の設定カテゴリ
https://pleasanter.org/manual/664450 / サイトパッケージの範囲
https://www.pleasanter.org/en/manual/site-package-export

# フェーズ4: 移行設計

## 4.1 移行設計（旧#10）

```markdown
以下の典拠から移行設計を作成し、ドライランまで行え。

## 典拠
db-design.yaml（凍結・store 付き）/ items.md / existing-tables.md /
meta-sync.yaml / 現物の記入済み Excel（実データ）

## フェーズA: 生成 — migration.yaml
store: 独自 の全テーブル・全列について:
- target: <テーブル.列>
  source: <Excel シート.列 | 既存テーブル.列 | 新規（移行なし）>
  transform: <変換規則。無変換なら none>
  on_blank: <空欄時の扱い。required: true の列は「補完値」か「行を除外」を
             明記せよ — 「エラーにする」で逃げるな>
  on_invalid: <型不正時の扱い>
  evidence: [ITEM-xx | REQ-xx]
- 「新規（移行なし）」には理由必須。
- メタ情報の初期値: meta-sync.yaml の各行について、移行時に Pleasanter
  アイテムをどう初期生成するか（同期ジョブが生成 / 移行スクリプトが生成 /
  不要）を書け。

## フェーズB: 検証（サブエージェント1体。ファイルパスのみ。編集禁止・報告のみ）
- 全数: store: 独自 の全列が migration.yaml に現れるか
- 幽霊: source の実在（Excel の該当シート・列が現物にあるか / 既存テーブルの実在）
- required 整合: required: true の列の on_blank が補完/除外で確定しているか

## フェーズC: ドライラン
現物 Excel のサンプル（可能なら全件）を migration.yaml に従って変換し、
SQLite（DDL 構築済み）に投入せよ。
投入後、scenario/*.sql を実行し、移行データ上でシナリオが回ることを確認せよ。
エラーは握りつぶさず件数で報告。

## 報告
- 対象列数 / 移行なし宣言 <件数> / 幽霊 <件数> / required 未確定 <件数>
- ドライラン: 投入 <成功行数>/<総行数> / 型エラー <件数> / required 違反 <件数> /
  シナリオ実行 <成功数>/<総数>
- 判定: (a)全数カバー (b)幽霊0 (c)投入成功+全シナリオ実行可 を ○×
```

## 4.2 キー解決フェーズの生成（閉域用・移行リハーサル）（旧#10a）

閉域でローカル DB しか触れない場合、外部テーブルの本番キー対応は原理的に
ローカルで確定しない。「未確定」を「当日に実行する突合手順」に変換する。

```markdown
これは「実行」ではなく「生成」のタスクである。実 DB への接続は不要であり、
出力はテキスト成果物のみ。実行は移行当日に人間が行う。
「実 DB が無いのでできない」という報告は不合格である。

migration.yaml で「外部テーブルのキー対応が未確定」とされた各件について:
1. キー解決 SQL を書け: Excel 識別値（取込済みの一時テーブル）と既存テーブルを
   突合する SELECT。既存テーブルのスキーマは existing-tables.md に定義がある —
   それを典拠に書け。
2. unmatched レポート SQL を書け: 一致しなかった識別値の全件出力。
3. 手順書 rehearsal/README.md を書け: 実行順 / 各ステップの確認ポイント /
   失敗時のやり直し方法 / 合否述語（実 DB での unmatched 0 で合格。1 件以上は
   移行を止め、件ごとに人間が補完 / 除外を決める。自動で倒すな）。
4. 構文検証: existing-tables.md からスタブテーブルをローカル SQLite に立て、
   書いた SQL が文法的に実行可能なことを確認せよ（結果の意味は問わない）。

出力: rehearsal/key-resolution.sql / rehearsal/README.md / 構文検証の実行結果
```

報告チェック4点: 全件に3点セットが揃ったか / 突合キーの幽霊 / 手順書の体裁 /
述語が「実 DB での unmatched 0、1件以上は人間判断」の形か。
※「実DBじゃないから」と何も生成しない縮退が実測された — 冒頭の宣言はその対策。
※ローカル実行の「未一致0」は循環か空振りで無意味。合否は実 DB でのみ確定する。

前倒し策: 既存テーブルの識別値カラムの distinct 値一覧だけを、検証環境に
触れる現場の人に抽出してもらう（キー列だけなら持ち出し制約を通りやすい）。

# フェーズ5: 実装（段3）

## 5.1 バックログの物質化・再生成（旧#12 — 再生成対応の現行版）

初回生成・再生成の両対応（既存 backlog が無ければ全行「未着手」で生成）。

```markdown
backlog.md を再生成せよ。
1. process-design.yaml から impl: 独自 または バッチ の全 process を抽出し、
   `PROC-ID / name / impl / 1行説明 / 状態` の表にせよ。
   既存 backlog.md があれば状態列を PROC-ID で突合して引き継げ
   （完了証拠付きの「完了」を落とすな）。無ければ全行「未着手」。
2. mjs 改修リストの節: 既存の内容を保持し、解消済みの行があれば
   完了に更新せよ（証拠1行付き）。既存が無ければ meta-sync.yaml と mjs を
   突合して矛盾を再検出し、実在しない参照は呼び出し経路の有無で
   デッドコード判定して導出せよ（`ファイル:関数 / 種別 / 状態`）。
3. 「引き渡し成果物」の節: 既存の内容を保持せよ。
4. 直近の台帳変更を反映せよ: 新規に増えた実装アイテム・改修アイテムが
   載っていること、廃止済み PROC のアイテムが残っていないことを確認せよ
   （残っていたら除去し件数を報告）。
5. 並び順は依存があるものを先頭に、依存を1行で明記せよ。
報告: 実装アイテム <n>（独自 / バッチ / mjs 改修の内訳）/
状態内訳（完了 / 着手可 / 回答待ち / 未着手 / 改修）/ 除去 <n> /
次に着手すべき先頭アイテム1件
```

見直し（監査）は 5.2（行単位の深い監査）またはチェッカー集 C12（整合の
確認のみ）。再生成→C12 が軽い定番。

## 5.2 バックログの監査（「おかしそう」の特定）（旧#12a）

```markdown
backlog.md を典拠と突合して監査せよ。集計だけの報告は不可、行単位で示せ。
1. 全数突合: process-design.yaml の impl: 独自|バッチ の全 PROC が backlog に
   1行ずつ存在するか — 欠落 / 重複 / 幽霊（backlog にのみ存在）
2. 内容整合: 各行の name・impl が process-design.yaml と一致するか
3. 状態列: 「完了」行に完了証拠（scenario 一致の記録・コミット）があるか
4. 境界待ち節: 各行が当日資産と対応しているか（対応物の無い行 / 行の無い対応物）
報告: 欠落 / 重複 / 幽霊 / 不整合 / 証拠なし完了 / 境界待ちの過不足 の件数と該当行
```

## 5.3 実装セッションの定型ヘッダ（旧#11・基線対応版）

バックログ1件（PROC 1本 or mjs 改修ひと束）につき1セッション。状態は全部
ファイルにあるためセッションは使い捨てでよい。同一アイテムの修正ループ中は
切らない。

```markdown
## 典拠（凍結。変更が必要なら差分提案として報告、勝手に変えない）
db-design.yaml / process-design.yaml / meta-sync.yaml / items.md /
existing-tables.md

## 対象
<backlog.md の1行をそのまま貼る>

## 着手前ガード
対象の rule・reads/writes・該当 scenario に「未確定(Q-xx)」が含まれる場合は
着手せず、「回答待ち（Q-xx）」と報告して終了せよ。推測で埋めて進めるな。

## 完了条件
- PROC 系: scenario/<該当>.sql が実装経由で同じ結果になること。
- mjs 改修系: ビルド成功＋該当画面のスモーク（表示・保存）が通ること。
- テストを通すために台帳・シナリオ側を書き換えることを禁ずる。

## 完了時
backlog.md の状態列を更新せよ。
```

## 5.4 scenario の鮮度回復（未確定の解消・再生成）

scenario/*.sql は処理設計時点の知識で凍った**導出物**。後続の台帳
（meta-sync・existing-tables の追記等）に答えが入っても自動では反映されない。
実装が「取得元不明」で止まったらこれで再生成する。
※「テストを通すために書き換えるな」との区別: 変更の根拠が**台帳にある**
再生成は正当、**実装の都合**でオラクルを曲げるのが禁止。

```markdown
scenario/*.sql の未確定を解消せよ。scenario は台帳からの導出物であり、
生成時点の古い知識で凍っている。現行の全台帳を典拠に再生成する。
典拠: process-design.yaml / db-design.yaml / meta-sync.yaml /
existing-tables.md / items.md
1. 未確定を含む scenario を列挙し、各件の「わからない」の対象
   （どのテーブル.列の取得元か）を特定せよ。
2. 各件の取得元を現行台帳から解決せよ:
   - meta-sync.yaml の source / existing-tables.md（alias 含む）/
     db-design.yaml の evidence: {external}
3. 解決できた件は SQL を書き直し、SQLite で実行して通ることを確認せよ。
4. 解決できない件のみ「未確定: どの台帳にも取得元が無い / 何が決まれば
   書けるか」として報告せよ。台帳に答えがあるのに未確定と報告することを
   禁ずる — 確認した台帳と該当行を引用せよ。
報告: 未確定だった <件数> / 台帳から解決 <件数> / 真の未確定 <件数>（内容）
```

## 5.4a 台帳変更後の scenario 再生成（波及分のみ）

5.4 が「未確定の解消」主眼なのに対し、こちらは「台帳が動いた後の追従」
主眼。波及分のみに絞る（スコープを絞る原則）。台帳が git 管理に
なったため、変更の特定は git に任せられる。

```markdown
台帳変更後の scenario 再生成を行え。変更に無関係な scenario は触るな。
典拠: process-design.yaml / db-design.yaml / meta-sync.yaml /
existing-tables.md / items.md（すべて現行版）

1. 変更の特定: 直近の台帳変更を列挙せよ。変更内容が指示されていない場合は
   git log / git diff で 02_metadata/ 配下の台帳の直近変更を確認して
   特定せよ（変更されたテーブル・列・PROC・DEC を列挙）。
2. 波及の逆引き: 変更された要素を reads/writes・evidence・参照に持つ
   scenario を全数列挙せよ。列挙外の scenario を書き換えることを禁ずる。
3. 該当 scenario を現行の全台帳から書き直し、DDL 構築済みの SQLite で
   実行して通ることを確認せよ。
4. 書き直しの過程で取得元が解決できない箇所が出たら、
   「未確定: どの台帳にも取得元が無い / 何が決まれば書けるか」として
   報告せよ。台帳に答えがあるのに未確定と報告することを禁ずる —
   確認した台帳と該当行を引用せよ。
5. 規律: 変更の根拠が台帳にある再生成は正当。実装の都合に合わせて
   scenario（オラクル）を曲げることを禁ずる。

報告: 変更要素 <n> / 波及 scenario <n>/<総数> / 書き直し・実行 <成功>/<n> /
未確定 <n>（内容）/ 触らなかった scenario <n>
```

## 5.5 メトリクス定義の抽出と推測の巻き戻し

scenario 再生成で「メトリクスの分子・分母が不明」「推測で解決した」が出たとき。
推測はオラクル（scenario）に残してはならない。メトリクス定義は現物 Excel の
計算式が正解を持っている（「定義が無い」のではなく「数式セルを掘っていない」）。

```markdown
1. 推測解決の巻き戻し: 「推測で解決した」各件について、
   - 推測の内容と、書き込まれた scenario の箇所を列挙せよ
   - scenario 上は「未確定」に戻せ。推測をオラクルに残すことを禁ずる
   - 各件を「提案 / 確認方法（どの実データ・どの Excel 式で白黒つくか）」の
     ○×リストに変換せよ
2. メトリクス定義の抽出: 分子・分母が不明な各メトリクスについて、
   現物 Excel の該当セルの計算式を探せ:
   - 見つかった → 式を引用し、列レベルの定義（分子= / 分母= / 除外条件= /
     集計期間=）に翻訳して提案せよ。source: <シート名.セル> 必須。
     実測根拠なしの提案は禁止。
   - 見つからない → 「定義がどこにも存在しない」として人間定義待ちへ
     （= 現行 Excel でも計算されていない新規指標の発見でもある）
3. 確定分は requirements.md の DEC-xx に記録し、rule と scenario に反映せよ。
報告: 推測巻き戻し <件数> / Excel 式から提案 <件数> / 人間定義待ち <件数>
```

## 5.5a 人間定義待ちメトリクスの2段仕分け（定義を書く前に）

人間定義待ちが多い場合、①式探索が浅かった（現に表示されている値なら出所は
必ずある — 死亡参照と同じ原理）②新規指標の定義は業務側の決定であり
自分で発明しない、の2点から仕分けを先に。

```markdown
人間定義待ちの各メトリクスについて、定義の前に2段で仕分けよ。
1. 現行観測の再走査: 各メトリクスが現行業務のどこか（Excel の別シート・
   別ファイル・帳票・報告資料・手計算メモ）に表示・報告されていないか、
   前回より広い範囲で再走査せよ。
   - 表示されている → 出所を特定し、式を引用して「Excel 式から提案」に昇格
   - どこにも無い → 「新規指標」確定
2. 新規指標の各件: evidence の REQ 原文を引用し、以下を付けて
   「業務側への質問リスト」として整形せよ:
   - その REQ は本当にこの指標を要求しているか（要求している / 拡大解釈の疑い）
   - 定義の提案（分子= / 分母= / 除外= / 期間=）+ 根拠 + 確度
   現場が○×で答えられる形にすること。
報告: 昇格 <件数> / 新規確定 <件数>（うち拡大解釈疑い <件数>）
```

## 5.5b どっちつかずの決着 — 値の突合（※実データに触れる環境がある場合のみ）

「候補は見つかったが同一と断定できない」の中間状態が出たとき
（2択の分類語彙に置き場が無いと曖昧化する）。メトリクスの同一性は
**候補式を実データで評価し、現物の表示値と突合**すれば数字が答える。
**閉域で値に触れない場合は 5.5d（参照構造の追跡）を使うこと — こちらが正。**

```markdown
どっちつかずの各件について、1件ずつ以下の形式で報告せよ。
「可能性がある」等の中間表現を禁ずる。
- メトリクス名 / 出所候補（シート.セルと式を引用。無ければ「候補なし」）
- 候補あり: 候補式を実データで評価し、現物に表示されている値と突合せよ。
  一致 → 「昇格確定」（この式が定義） / 不一致 → 両方の値を引用し「別物」
- 候補なし → 「新規確定」
判定は 昇格確定 | 別物 | 新規確定 の3値のみ。
突合が実行できない件は、何が足りないかを1行で書け。
報告: 全行の表 + 集計（昇格確定 / 別物 / 新規確定 / 突合不能）
```

## 5.5c 突合の前提宣言（5.5b が決着しないとき。※同じく値に触れる環境限定）

突合が繰り返しどっちつかずを返す原因は前提の壊れ: ①母集団未特定のスポット
計算 ②サンプルデータでの比較（fixture 未一致0と同じ無意味比較）。
対処は前提の物質化 — 実データの名指し（人間。ファイル名から real/sample は
モデルに区別不能）と、評価より先の前提宣言の強制。

```markdown
値の突合をやり直す。前提を宣言しない突合は無効である。
0. データの認定: 突合に使ってよいのは <実データファイル名を人間が名指し> のみ。
   サンプル・テンプレート（<名指し>）の使用を禁ずる。使用ファイル名を報告に明記。
1. 各メトリクスについて、評価の前に「突合の前提」を宣言せよ:
   - 母集団: どのシート・どの行範囲が対象か
   - 絞り込みキー: 何で絞るか（期間・案件・部署等）。現物の表示セル周辺
     （見出し・フィルタ・集計行の位置）から根拠を引用せよ
   - 比較対象: 現物のどのセルの表示値と比べるか
2. 宣言した母集団の上で候補式を評価し、表示値と比較せよ:
   一致 → 昇格確定 / 不一致 → 両値と前提を添えて「別物」/
   前提が特定できない → 「前提不明: 何が分かれば突合できるか」
報告: 各件 `前提（母集団/キー/比較セル）/ 評価値 / 表示値 / 判定` の表。
前提欄が空の行は無効。
```

前提不明で残った件は「母集団の定義が現物から読み取れない」という発見であり、
現場への質問（この指標の分母集団は何か）に変換する。

**引き渡し原則（「境界待ち」の廃止）**: 「検証環境に接続できたら」という
起きないイベントへの参照をコンテキストに残さない。ローカルで閉じない検証は
「待ち」でなく**完成した納品物（移行手順書パッケージ）として閉じる** —
実行順・合否述語・記入欄付きの1枚に整形し、実行は移行実施者が移行手順の
一部として行う（このプロジェクトの管理対象外）。未検証の前提は設計書の
「制約・前提」節に既知の制限として記載（開いたタスクでなく引き渡す事実）。
移行後に旧 Excel と併存する期間があれば並行運転の突合が最終検証を兼ねる。

```markdown
backlog.md の「境界待ち」節を「引き渡し成果物」に改めよ。
1. 各行を成果物として記述し直せ: rehearsal/ 一式・verification/
   metrics-check.sql 等は「移行手順書パッケージ」であり、手順書が揃っている
   行は状態を「完了（引き渡し）」として閉じよ。実行は移行実施者が
   移行手順の一部として行う。
2. 「検証環境に接続できたら」「接続時に」という表現を backlog・rehearsal・
   台帳・設計書のすべてから除去し、「移行実施者が移行手順の一部として
   実行する」に書き換えよ。
3. 未検証の前提（メトリクス定義は実データと未突合 等）は設計書の
   「制約・前提」節に既知の制限として1行ずつ記載せよ。
報告: 表現の書き換え <箇所数> / 引き渡し成果物として完了 <件数> /
制約として記載 <件数>
```

## 5.5d メトリクス定義の確定 — 参照構造の追跡（閉域の正攻法）

目的は定義（分子・分母・母集団）の確定であって値の検証ではない — 手段に
目的を乗っ取らせない。SQL への翻訳に必要なのは定義であり、定義は式の参照
構造を静的に追跡すればローカルで閉じる。同一性の判定基準を「値の一致」から
**「参照構造の一致」**（連鎖の根が items / existing-tables の項目に対応付くか）
に差し替える。値の最終確認は境界待ちへ予約。

```markdown
各メトリクスについて、値の計算・突合はするな（実データに接続できない前提）。
代わりに数式の参照構造を根まで追跡し、定義として抽出せよ。
1. 表示セルの式を引用し、参照先のセル・範囲を列挙せよ。
2. 参照先がさらに式なら、その参照を辿り、生データ（入力セル・外部参照）に
   到達するまで連鎖を書け（表示セル → 中間式 → 生データ の形）。
3. 到達した生データを items.md / existing-tables.md の項目に対応付けよ。
   対応付かない参照は「出所不明」と書け。推測で埋めるな。
4. 定義として整形せよ: 分子=<列・条件> / 分母=<列・条件> / 除外条件= /
   集計単位（何ごとに1行か）=
5. 確からしさを付けよ: 高（連鎖がすべて解決）/ 中（一部不明）/ 低。
値での最終確認は verification/metrics-check.sql（引き渡し成果物）として
整形し、移行手順書パッケージに同梱せよ。
報告: 定義確定 <件数> / 出所不明を含む <件数> / 現場質問行き <件数>
```

## 5.6 指摘の正規化（対話修正を台帳へ載せ直す）

対話で「ゴリッと」直させた修正はその場のファイルに直接当たっている —
導出物（scenario・設計書）への直接修正は次の再生成で消える。原則:
**指摘は正本の台帳に反映し、導出物は台帳から再生成**。
ルーティング: 業務規則・定義の決定→DEC-xx（rule から引用）/ 項目の事実→items /
既存テーブル→existing-tables / テーブル・列→db-design / 処理→process-design /
同期→meta-sync / 移行→migration / 実装のみ→台帳反映不要 /
体裁→生成テンプレを直して再生成。
判別述語: 「その指摘は何についての事実・決定か」「将来の別セッションが
知らないと困るか」。

```markdown
今セッションで指摘・修正した内容を、以下の手順で資料に正規化せよ。
1. 一覧: `指摘内容 / 修正内容 / 現在どのファイルに反映されているか`
2. ルーティング規則で正本の台帳へ振り分けて反映せよ。特に:
   - 業務規則・定義の決定は requirements.md に DEC-xx として追記し、
     process-design の rule から DEC-xx を引用する形に直せ
   - scenario・設計書（導出物）への直接修正は、正本の台帳に反映し直し、
     導出物は台帳から再生成せよ。導出物への直接修正を残すな
   - 実装コードのみの修正は「台帳反映不要」と明記せよ
3. 台帳が動いた分: 該当 scenario を現行の全台帳から書き直して実行し、
   設計書を台帳から再生成せよ。検証（カバレッジ＋使用審問）を1回かけて
   差分のみ報告せよ。
報告: 指摘 <件数> / 反映先の内訳 / 導出物直接修正の巻き戻し <件数> / 検証差分
```

## 5.7 未確定のライフサイクル（質問台帳への駐車）

回答待ちの未確定に終着駅が無いと、検証のたびに新規発見の顔で再浮上し、
モデルが区別のための新語（「真の未確定」等）を発明し始める。
語彙を固定し、回答待ちは questions.md に駐車する。
解消の唯一の経路: 現場回答 → Q-xx を回答済み → DEC-xx に変換 → rule/台帳へ。

```markdown
未確定の語彙を以下に固定せよ。「真の未確定」等の新しい分類語を発明することを禁ずる。
1. questions.md（質問台帳）を作れ: 現場質問行きの各件に Q-xx を振り、
   `Q-xx / 内容 / 宛先 / 状態(回答待ち | 回答済み)` で登録せよ。
2. 台帳・scenario 上のすべての未確定を2分類に振り分けよ:
   - 未確定(Q-xx) — 登録済みの回答待ち。以後の検証・整理では
     「既知・回答待ち N 件」と集計だけせよ。新規発見のような個票報告を禁ずる。
   - 新規未確定 — Q-ID の無いもの。これだけ個票で報告:
     `内容 / なぜ今出たか（正規化の波及 | 以前からあったが未登録 | その他）`
3. 現在「未確定」「真の未確定」等として存在する全件を、この2分類に振り分けよ。
報告: 既知（回答待ち）<件数> / 新規 <件数>（個票付き）
```

## 5.8 現場回答の反映（Q → DEC → 台帳 → scenario の一本道）

原則: 回答は原文で記録（出所主義）/ 波及は Q-xx の逆引きで機械列挙（漏れ防止）/
検証は差分のみ。「残る回答待ち」件数が進捗メーター（0 で外部依存消滅）。

```markdown
現場打ち合わせの回答を反映せよ。回答メモは末尾に貼ってある。
各回答について順に:
1. questions.md の該当 Q-xx に、回答の原文メモ・回答者名・日付を記録し
   「回答済み」にせよ。
   - その場で「提案に○が付いた」件は、提案内容を回答として記録せよ。
   - 「保留・持ち帰り」の件は「回答待ち」のまま残し、持ち帰り内容を追記せよ。
     勝手に回答済みにするな。
2. requirements.md の「## 決定事項」に DEC-yy として追記せよ
   （決定内容 / 出所: Q-xx への回答・回答者・日付）。
3. 波及の反映: Q-xx を参照している未確定を台帳・spec・scenario から逆引きして
   全件列挙し、DEC-yy を引用する形で解消せよ。列挙外の反映を勝手にするな。
4. spec の全 stage が解消されたら、該当 scenario を stage ごとの CTE を持つ
   SQL として書き直し、SQLite で実行して通ることを確認せよ。
5. backlog: 回答で解禁される実装アイテムの状態を「着手可」に更新せよ。
報告: 反映した Q <件数> / 保留 <件数> / DEC 追加 <件数> /
解消した未確定 <件数> / scenario 実行 <結果> / 解禁アイテム <件数> /
残る回答待ち <件数>
---
Q-xx: <回答内容> / <回答者> / <日付>（以下、打ち合わせメモをそのまま）
```

メモは1件1行の走り書きでよい（整形・記録・波及はプロンプト側の仕事）。
保留が出ても壊れない（答えの出た分だけ進む）。反映が変なら git で台帳を
戻して再実行。

## 5.8a 回答提案（セルフ○× — 認知負荷の削減）

質問の多くは本人が答えを持っており、足りないのはゼロから言語化する気力。
言語化をモデルに任せ、本人は○×だけにする（仮説提示の型を質問リストに適用）。
「要追認」フラグが安全弁 — 今決めて作業を解禁しつつ、業務影響の大きい決定は
現場打ち合わせで追認を取る（出所は「本人決定(要追認)」と正直に記録）。
打ち合わせは「提案不能+×+要追認の確認」だけに縮む。

実測された失敗2つ: ①提案が業務要件を勝手に変える（縮退解の親玉 —
要件保存則で封じる）②資料を読まない浅い提案が却下後も再生産される
（渡す≠読む + 却下の未記録）。対策: 読了証拠の強制（Q ごとに関係資料から
事実3点を出典付き引用 — 無い提案は無効）と却下台帳 rejected.md
（×の付いた案と理由を記録し、同方向の再提案を禁止 — 進歩を単調にする装置）。
提案プロンプトには以下を冒頭に置く:

- 0. 却下台帳: rejected.md に却下済み案（Q-xx / 要旨 / 理由）を記録し、
  提案前に必ず照合。台帳にある方向の再提案を禁ずる。
- 1. 読了証拠: 各 Q の提案の前に、関係資料（REQ 原文 / DEC / spec /
  meta-sync / items / 現物 Excel / 描画コード）から最も関係の深い事実3点を
  出典付きで引用せよ。読了証拠の無い提案は無効。
- 2. 提案は読了証拠の上に組み立てる。要件保存則は下記どおり。

```markdown
questions.md の回答待ち各件について、私が○×だけで済む「回答提案」を作れ。
判断は私がする。根拠つきの推測はよいが、根拠なき断定をするな。

## 要件保存則（違反する案を出すことを禁ずる）
- 回答案は requirements.md（REQ・スコープ宣言・既存 DEC）を1文字も変えずに
  成立するものに限る。要件の追加・削除・緩和・読み替えを含む案は無効。
- 各回答案に「要件整合」欄を必須で付けよ: 関係する REQ-ID の原文を引用し、
  この案が原文のままで成立することを示せ。示せない案は出すな。
- 要件を変えなければ答えが出ない Q は「提案不能（要件変更が必要）: 何を
  どう変える必要があるか1行」とせよ。要件変更は人間と現場の判断であり、
  回答の形に混ぜ込むことを禁ずる（この判明自体が収穫 — 現場への持ち込みが
  「決めてください」から「要件のここが両立しません」に変わる）。

各 Q:
1. 参照できる材料を全部当たれ: 要件書原文 / 既存 DEC / 現物 Excel の式・見出し /
   プロトタイプの描画コード / 実データの傾向 / 既存テーブルの使われ方。
2. 回答案: `Q-xx / 回答案（決定文として使える1〜2行）/ 根拠（出典引用）/
   確度(高|中|低) / 反対案あれば1行 / 要追認(有|無) / 要件整合（REQ 原文引用）`
3. 材料が無い Q だけ「提案不能: 現場でしか分からない理由1行」。
○×後の反映:
- ○ → 本人決定として記録（回答者=本人・日付・要追認併記）→ DEC-yy 化 →
  Q 参照の逆引きで波及を全件列挙してから解消 → 該当 scenario 再生成・実行 →
  backlog 解禁、まで一気に。
- 修正付き○ → 修正後の文面で同様に。 / ×・提案不能 → 回答待ちのまま（現場行き）。
報告: 提案 <件数>（確度内訳）/ 提案不能 <件数> /
○×後: 決定 <件数>（うち要追認）/ 現場行き残 <件数> / 解禁 <件数>
```

## 5.9 複雑処理のミニ台帳分解（同じ場所で繰り返し止まるとき）

同じ処理で内容の違う停止が繰り返される場所は**複数知識源の合流点**であり、
多段導出が PROC 1行の rule に圧縮されているサイン。スポット修正でなく、
その処理専用のミニ台帳（stage 分解の導出仕様）を作る。
効能: 穴が設計時に全部見える（stage ごとの evidence 枠）/ 止まる単位が
局所化する（scenario が stage 別 CTE になり、名指しで止まる）。

```markdown
<対象処理> を多段導出のミニ台帳に分解せよ。<spec名>.md を作成:
- stage1 取得: 入力（テーブル.列・絞り込み条件）→ 出力（行集合の定義）
- stage2 構成: 軸・セル値等の導出式
- stage3 分類: 区分の境界値。境界の出所を必須で付けよ
  （Excel 式の引用 | DEC-xx | 出所なし→未確定(Q-xx)化）
- stage4 出力: 最終表示の内容と、どの区分の組合せに対応するか
各 stage に evidence（REQ / ITEM / DEC / Excel セル）必須。
書けない箇所は未確定(Q-xx) にせよ。推測で埋めるな。
完成後:
1. spec から該当 scenario を stage ごとの CTE を持つ SQL に書き直し、
   SQLite で stage 単位に実行確認せよ。
2. process-design.yaml の該当 PROC の rule に「詳細: <spec名>.md」と参照を書け。
   分割すべきなら分割を提案せよ（勝手に分割するな）。
報告: stage 別 evidence 充足（済 / 未確定Q）/ stage 別実行結果 /
今回の停止がどの stage の穴だったか
```

## 5.9a stage の現物抽出（上流 stage が未確定のとき）

stage は依存順に上流から埋める。現に表示されているものには軸・セル値・出所の
実体が必ずある — 頭で決めず現物から抽出（参照構造の追跡をマトリクスに向ける）。
抽出の向きは**表示から根へ**（現物→構成(stage2)→参照連鎖の根(stage1)）。

```markdown
<spec名>.md の stage1・stage2 を現物から抽出せよ。推測で埋めることを禁ずる。
1. 現物の特定: この表示が現在存在する場所（Excel のシート・範囲、または
   既存画面の HTML と対応する mjs）を特定せよ。
2. stage2（構成）の抽出: 行軸・列軸・セル値が何かを、Excel の見出し・式、
   または mjs の描画コードから引用付きで抽出せよ。
3. stage1（取得）の抽出: セル値の参照連鎖を根まで辿り、どのテーブル.列を
   どの絞り込み条件で取った行集合かを特定せよ。根は existing-tables.md /
   items.md / meta-sync.yaml の項目に対応付けよ。
4. 現物から読めない箇所のみ 未確定(Q-xx) 化し「何が分かれば確定するか」を1行。
報告: stage1・2 の確定内容（引用付き）/ 残った未確定 Q /
stage3・4 への波及（境界値・セル表示が判明したら併記）
```

## 5.9b 典拠の権限分割（「一致しない」と言い張る膠着の解除）

現行 Excel が外部ツール出力の貼り付けで DB とリンクしない場合、
「Excel を根まで辿って DB に対応付けよ」は**存在しない対応の探索命令**であり、
モデルは忠実なほど「一致しない」と言い続ける。オントロジー等の中間層を
挟んでも解決しない（対応が無い事実は層を挟んでも消えず、コンテキストを
食って要件内容が押し出される副作用だけ残る — 層は撤去する）。
解は**側面ごとの典拠権限の宣言**。

```markdown
<spec名> を以下の典拠権限で書き直せ。権限外の突合を禁ずる。

## 前提の宣言（spec の冒頭にこのまま書け）
「現行 Excel のデータは外部ツールの出力を貼り付けたものであり、DB とは
リンクしない。Excel の値と DB が一致しないのは正常であって欠陥ではない。
両者の一致検証を試みることを禁ずる。」

## 典拠の権限分割（側面ごとに、これ以外の典拠を参照するな）
- stage1（データの出所）: 新設計の台帳（db-design / meta-sync / migration）のみ。
  ツール由来データは <外部定量値の取込列> に着地する。
  Excel の取得元を辿って対応付けることを禁ずる。
- stage2（軸・セル値の意味）: 現物 Excel / プロトタイプからの引用。
  値ではなく構造と意味だけを読め。
- stage3（分類の境界値）: Excel 式の引用 または DEC-xx。無ければ 未確定(Q-xx)。
- stage4（表示・並び順・レイアウト）: プロトタイプ HTML に従え。
  プロトタイプが決めていない部分（可変行の並び順等）は、無言でスキップせず
  「提案 + 根拠 + 選択肢2〜3」を出して人間の○×を仰げ。

報告: stage 別の確定内容（典拠の引用付き）/ stage4 の提案リスト / 残 Q
```

原理: **典拠は「どれを見るか」だけでなく「どの側面について権威か」まで
宣言する**。宣言が無いとモデルは全典拠の全側面を整合させようとし、
実在しない対応の上で膠着する。

## 5.9c 動的描画の表の抽出（「プロトタイプに無い」の誤判定）

拡張HTML＋mjs 構成では可変行の表は JS が実行時に描画する — 静的 HTML には
空のコンテナしか無く、静的走査は「無い」と誤報告する（動的キー生成の
走査漏れと同族の盲点）。人間の指差し1回（画面/ファイル名）で解ける。
描画コードは表示仕様の最良の典拠 — 並び順・可変行の扱いはコードから引用で確定。

```markdown
プロトタイプの表は実在する（人間が確認済み）。「無い」という判定は誤りである。
対象: <画面名 or ファイル名を人間が指定>
1. 静的 HTML に表のマークアップが無い場合、それは mjs による動的描画である。
   コンテナ要素（div 等の id / class）を HTML から特定し、そこに描画している
   mjs のコード（DOM 生成・innerHTML・テーブル構築関数）を特定して引用せよ。
2. 描画コードから構造を抽出せよ: 行・列はどのループ・どのデータキーから
   生成されるか / セルには何を表示しているか / 並び順はどこで決まるか。
   抽出結果を spec の stage2（構成）・stage4（表示）に反映せよ。
3. 以後の規律: 「プロトタイプに無い」と判定する場合は、探索したファイル一覧と
   探索方法（静的マークアップ / 動的描画コードの両方）を添えよ。
   両方を探さずに「無い」と報告することを禁ずる。
報告: コンテナの所在 / 描画コードの所在（ファイル:関数）/ 抽出した構造
```

## 5.9d 判断カード（未確定を「考える仕事」から「選ぶ仕事」へ）

抽出で埋まらない未確定＝設計判断そのもの。手転記環境では相談の往復ができない
ため、業務PC側でその場判断できる3択カードに変換する。

```markdown
<spec名> の各未確定について、私がその場で判断できる「判断カード」を
1件ずつ作れ。推測で決めるな — 判断は私がする。
各カード:
- 未確定の内容（1行）と、関わるコードのデータキー・Excel 箇所（引用）
- 選択肢を3方向で具体化:
  A) 既存の設計に対応がある: db-design / meta-sync / existing-tables から
     候補列を実名で挙げ根拠1行。無ければ「候補なし」と書け（ひねり出すな）。
  B) 新列・新規則が必要: 列名・型・着地テーブルの具体案と DB 差分提案の下書き。
  C) 業務判断が必要: 現場に聞く質問文の下書き（○×で答えられる形）。
- 推奨（A|B|C）と確度、理由1行。
選択後の反映: A→spec と meta-sync に結線を記録 / B→DB 差分提案（承認後反映）/
C→questions.md に Q-xx 登録。
報告: カード <件数> / 推奨内訳 A / B / C
```

## 5.9e 判断カード反映の完了確認と質問アジェンダ整形

**反映の完了確認（カード選択後）:**

```markdown
反映の完了確認。以下を報告せよ:
1. questions.md の全行を `Q-xx / 内容(1行) / 状態` で一覧にせよ。
   期待件数に足りない場合、未登録の C カードを列挙して登録せよ。
2. A カード: spec と meta-sync への結線が記録済みか（該当行を引用）。
3. B カード: DB 差分提案の下書きが存在するか（提案の1行要約）。
報告: Q 登録 <件数>/<期待> / A 結線 <済|未> / B 下書き <件数>/<期待>
```

**質問アジェンダの整形（現場打ち合わせ用）:**

```markdown
questions.md の回答待ち全件を、打ち合わせ用のアジェンダ1枚に整形せよ:
- 各件: 質問文（○×か選択で答えられる形）+ 背景1行 + 決まると何が進むか1行
- 関連する質問（同じ表・同じ指標のもの）は隣接させてグルーピングせよ
- 冒頭に「所要目安 15分 / 目的: 設計確定のための業務定義の確認」と書け
```

## 5.10 設計変更の実装への反映（台帳→scenario→backlog の再導出）

設計変更を実装へ反映するときはコードを直接直しに行かない。完了条件が
「scenario 一致」である構造を使い、オラクル（scenario）の更新 → backlog
再生成 → 完了済みの再判定、で影響範囲を機械的に確定する。「改修行き」の
件数が設計変更による手戻りの正確な範囲。実装自体は 5.3 で1件ずつ。

```markdown
設計変更（承認済み差分）を実装系の成果物へ反映せよ。コードを直接
修正するな — 実装の修正は backlog のアイテムとして行う。
1. 波及の確定: 今回の設計変更（DEC・台帳差分）を evidence に持つ
   scenario を逆引きし、現行の全台帳から再生成して SQLite で実行せよ。
2. backlog.md を再生成せよ: process-design.yaml の impl: 独自|バッチ の
   全 PROC を対象に、既存の状態列を PROC-ID で突合して引き継げ。
   新規アイテム（メタ設計・同期由来・つなぎ込み）と消滅アイテムを
   差分で報告せよ。
3. 完了済みアイテムの再判定: 状態「完了」の各アイテムについて、完了根拠の
   scenario が手順1で変わったかを判定せよ:
   - 変わっていない → 完了のまま
   - 変わった → 実装経由で新 scenario を再実行し、一致すれば完了維持、
     不一致なら状態を「改修（設計変更反映）」に戻し、差分内容を1行付けよ
4. つなぎ込み残と新規同期ジョブが backlog に載っていることを確認せよ。
報告: scenario 再生成 <n>（実行 <成功数>/<n>）/ backlog: 新規 <n>・
消滅 <n>・完了維持 <n>・改修行き <n> /
次に着手すべき先頭アイテム1件（依存順・依存を1行明記）
```

# フェーズ6: 設計書生成（実装と並行可）

## 6.1 台帳からの設計書一式生成

設計書は執筆でなく**変換** — 内容は全部台帳にある。生成は何回でもタダなので
実装と並行してよく、乖離は再生成で消える。docs/design/ を git 管理すれば
再生成の diff が設計変更レビューになる。設計書側は手で直さない
（唯一の例外は 00-overview の散文 — 人間レビュー前提のドラフト）。

```markdown
台帳から設計書一式を docs/design/ に生成せよ。全文書の冒頭に
「本書は台帳からの自動生成。修正は台帳側に行うこと」と生成日を記せ。
台帳に無い情報を書き足すな。

1. 00-overview.md — アーキテクチャ概要: 着地点宣言・impl 内訳・meta-sync から
   責務分界（Pleasanter 標準 / 拡張HTML+独自テーブル / バッチ）を図
   （mermaid 可）と散文1〜2ページで。散文はドラフトであり、末尾に
   「要人間レビュー」と記せ。
2. 10-database.md — db-design.yaml から: テーブル一覧（用途1行）→
   各テーブルの列表（名前 / 型 / 必須 / store / 説明 = evidence の REQ を
   1行要約）→ FK・external 参照の一覧。
3. 20-process.md — process-design.yaml から: 処理一覧（種別・impl・trigger）→
   各処理の reads/writes（CRUD 表）と rule（DEC/REQ 引用付き）。
4. 30-integration.md — meta-sync.yaml とサイト構築仕様から:
   同期対応表（field / source / direction / sync）と Pleasanter サイト構成。
5. 40-migration.md — migration.yaml と rehearsal/ から:
   移行対応表と当日手順（キー解決・合否述語含む）。
6. 90-traceability.md — 全 REQ → テーブル.列 / PROC の対照表と、
   決定記録一覧（requirements.md の DEC-xx を転記）。
7. 自己検査: 各文書の件数（テーブル数・列数・処理数・REQ 数）を台帳と突合し、
   冒頭に「件数検査: 一致（テーブル N / 列 N / 処理 N / REQ N）」と記せ。
   不一致なら生成をやり直せ。
```

並行運用ルール: 再生成トリガーは「台帳が動いたとき」（差分提案の承認時）/
設計書は手で直さず台帳を直して再生成 / pending の提案は映らなくて正しい
（冒頭ハッシュが時点を保証）。

## 6.2 レビュー用 DB 設計書の生成（人間レビューに出す版）

6.1 との違いはレビュー最適化の2節 — **検証状態**（機械検証済みの範囲を数字で
示し、レビュアーに再確認させない）と**レビュー依頼事項**（人間判断で決めた点
だけを抜き出す）。原則「律速はレビュー容量 — 機械が判定できるものを人間に
見せない」の設計書版。レビューで受けた指摘は設計書に直接反映せず、
正本の台帳への差分（承認経由）→ 再生成で取り込む。

```markdown
db-design.yaml から、人間レビュー用の DB 設計書 docs/design/review-db.md を
生成せよ。台帳に無い情報を書き足すな。冒頭に「本書は台帳からの自動生成
（生成日）。修正は台帳側に行うこと」と記せ。

典拠: db-design.yaml / requirements.md（スコープ宣言・決定事項 DEC）/
existing-tables.md / items.md / speculative 採否・未確定決定の記録 /
questions.md

構成（この順で）:
1. 前提 — 規模の前提（同時ユーザー数・行数/年・運用年数・チーム人数）、
   スコープ宣言（対象外 REQ）、着地点宣言（store 方針の1行）
2. テーブル一覧 — テーブル名 / 用途1行（evidence の REQ を要約）
3. 各テーブル詳細 — 列表（列名 / 型 / 必須 / store / 説明 = evidence の
   REQ・ITEM の1行要約）、主キー・FK、external 参照は existing-tables.md の
   alias（業務上の呼び名）を併記
4. 既存テーブルとの関係 — external 参照の一覧（参照先 / 目的）。
   既存テーブルは変更しない（参照のみ）ことを明記
5. 決定記録 — requirements.md の DEC-xx を転記（決定内容 / 出所）
6. 検証状態 — 実施済みの機械検証の結果を数字で記せ:
   順トレース missing <n> / 削除審問の最終結果 <n>件 /
   フェーズD（DDL 実行 <成功|失敗> / シナリオ <実行数>/<要件数> /
   未使用 <n> / 書けなかった要件 <n>）。数字は実際の検証記録から転記し、
   記録が無い項目は「未実施」と書け。実施済みと偽ることを禁ずる。
7. レビュー依頼事項 — 機械検証で白黒がつかず人間判断で決めた点のみを列挙:
   - speculative の採用 <n>件（各: 要素名 / 採用理由1行）
   - 回答待ちの未確定のうち DB 設計に関わるもの（questions.md から Q-ID 付き）
   - 制約・前提（実データ未突合等の既知の制限）
   ここに列挙した点以外は機械検証済みであることを一文で添えよ。
8. 自己検査 — テーブル数・列数・DEC 数を台帳と突合し、冒頭に
   「件数検査: 一致（テーブル N / 列 N / DEC N）」と記せ。不一致なら生成を
   やり直せ。

報告: 生成パス / 件数検査 / レビュー依頼事項の内訳
（speculative <n> / 未確定 <n> / 制約 <n>）
```

レビュー後の反映は「指摘の正規化」の手順で行う（指摘→正本の台帳へ
ルーティング→導出物は再生成。設計書への直接修正を残さない）。

# 7. 整合性スイープ（要件変更後の全層検証）

凍結は「変更禁止」でなく「変更は差分提案・承認経由」— 正当な要件変更で台帳が
動くのは正常で、evidence はこのときの波及逆引きのためにある。変更後の不安は
スイープ1回で不確実性ごと消す（整合性は信じるものでなく測るもの）。
変更手順の定型: REQ 変更 → 波及の逆引き → 各台帳へ差分 → スイープ1回。

```markdown
要件変更後の全台帳整合性スイープを実行し、スコアカード1枚にまとめよ。
0. 変更の特定: 変更・追加・削除された REQ-ID を列挙し、それを evidence に持つ
   全要素（db-design / process-design / meta-sync / migration / scenario /
   questions.md）を逆引きして「波及範囲」として列挙せよ。
1. DB 層: 全 REQ（スコープ宣言の対象外を除く）が db-design.yaml で満たされるか
   （順トレース）と、全テーブル・列が根拠（REQ / structural / external）を
   持つか（削除審問）。全行検分の表を付けよ。
2. 処理層: 全 REQ がいずれかの process で実現されるか（カバレッジ）と、
   db-design.yaml の全列が reads/writes に現れるか＋幽霊参照の有無（使用審問）。
3. 同期層: meta-sync.yaml の source・field の実在 / 単一の正 / sync の
   process 実在 / mjs との読み書き方向の突合。
4. 移行層: store: 独自 の全列が migration.yaml にあるか / source の実在 /
   required: true の on_blank が補完・除外で確定しているか。
5. 実行層: DDL を SQLite で実行し、全 scenario/*.sql を実行せよ。
6. 運用層: backlog と process-design の全数突合（欠落 / 重複 / 幽霊）＋
   questions.md の Q 参照整合。
7. 設計書: 台帳から再生成し、件数（テーブル・列・処理・REQ）の台帳突合を付けよ。
報告（スコアカード）: `層 / 判定(○|×) / 検分した件数（行数・列数・REQ 数等の
母数） / ×の件数とパターン` の表
＋ 波及範囲のうち未更新のまま残っている要素の一覧
※母数 0 の層に○を付けることを禁ずる — 空の台帳は全ての検証を素通りする
（空虚な○）。母数 0 は「対象消失の疑い」として×扱いで報告せよ。
```

×の層はその層の既存手順（差分提案→承認→反映→当該層のみ再検証）で個別に閉じる。
スイープ全体の回し直しは不要 — ○だった層は変更に触れていないことが確認済み。

## 7b 後工程の作業中に上流設計の欠陥を見つけたときの定型

後工程（メタ設計・実装等）が上流の欠陥を見つけるのは検出器の正常動作。
禁じ手は「その作業セッションの流れで直させる」こと（別目的の作業中に
正本台帳を触るのは事故の温床）。作業を中断し、差分提案に物質化する。

```markdown
<作業名> の最中に、上流の設計の問題を発見した。<作業名> は一時中断し、
問題を差分提案の形に物質化せよ。勝手に直すな。
1. 問題の記述: `何がおかしいか / どの台帳のどの箇所か（引用）/
   なぜ問題か（衝突する要件・事実の引用）`
2. 正本の特定: この問題の修正はどの台帳に入るべきか
   （requirements の DEC | db-design | process-design | items |
   existing-tables | meta-sync）。
3. 波及の逆引き: 修正対象の要素を evidence に持つ全要素（他の台帳・
   scenario・backlog・設計書）を逆引きして列挙せよ。
4. 差分提案: `対象台帳 / 現状 / 変更案 / 根拠 / 波及 <n>件` の形で出せ。
   代替案があれば併記せよ。私の○×を待て。
承認後: 差分を反映 → 波及分（scenario は再生成して実行、設計書は再生成）を
更新 → 変更した層の検証のみ再実行（DB 層なら順トレース＋削除審問、
処理層ならカバレッジ＋使用審問）→ 差分のみ報告 → <作業名> を再開せよ。
報告: 問題 <内容1行> / 正本 <台帳名> / 波及 <n>件 / 差分提案（○×待ち）
```

## 7a ×層の閉じ方（処理層・運用層の定型）

**処理層（確定モードで最小差分）:**

```markdown
整合性スイープの処理層×を確定モードで解消せよ。process-design.yaml は
再生成せず最小差分のみ。
1. 分類: missing（変更後 REQ に対応する処理が無い・古い）/ 未使用列 / 幽霊参照。
2. missing: 変更後の REQ 原文を引用し、処理の追加または rule・reads/writes の
   更新で解消（evidence 更新必須）。典拠から書けない件は Q-xx 化せよ。
3. 未使用列: 書き漏れなら処理側を更新、本当に不使用なら「DB 差分提案（削除）」へ。
4. 幽霊参照は必ず解消。
5. カバレッジ＋使用審問を再実行し差分のみ報告。scenario に波及した分は
   現行の全台帳から書き直し、実行して通ることを確認せよ。
報告: 分類内訳 / 解消 / Q化 / DB差分提案 / 再検証判定
```

**運用層（自己完結版）:**

```markdown
運用層の×（backlog / questions の整合）を解消せよ。
1. ×の各パターンの内容を1行ずつ示せ。
2. backlog.md の再生成: process-design.yaml から impl: 独自|バッチ の全 PROC を
   `PROC-ID / name / impl / 1行説明 / 状態` の表として作り直せ。
   既存 backlog.md の状態列を PROC-ID で突合して引き継げ。
   mjs 改修の節・引き渡し成果物の節は既存の内容を保持せよ。
3. questions.md の整合: 台帳・scenario 中の 未確定(Q-xx) 参照がすべて
   questions.md に実在する Q に対応するか全件確認し、破れた参照は張り直せ。
   参照先が消えている Q は理由を報告せよ。
4. 再監査: backlog と process-design の全数突合（欠落/重複/幽霊）、
   「完了」行の完了証拠の有無、Q 参照の整合を確認し判定を報告せよ。
報告: パターン内容 / 処置 / 再監査判定（○|× と件数）
```

# 8. 現在地の確認（迷子・パニック時の再開点）

「どこから再開するか」を覚えている必要はない — 状態は全部ファイルにある。
迷ったら毎回これを打つ。ファイルが答える。

```markdown
現在地を報告せよ。判断や修正はするな、読むだけでよい。
1. backlog.md: 状態別の件数（完了 / 着手可 / 回答待ち / 未着手）と、
   依存順で次に着手すべき先頭アイテム1件
2. questions.md: 回答待ちの件数と、回答提案（○×待ちのカード）の有無
3. 直近の検証判定（スイープ等）
報告: 上記3点のみ。
```

# トラブルシューティング（横断・フェーズ非依存）

## T1. 集計不一致の個票突合（旧#13）

2つの走行の集計が両立しないとき、どちらかが仕事をしていない。
集計の食い違いは集計では解けない — 1件ずつの個票で白黒をつける。

**形A: 元リストが手元にある場合**

```markdown
<過去の集計で挙がった N 件のリスト> について、1件ずつ以下を報告せよ。
「0件だった」のような集計だけの報告は不合格である。
各件: <対象の特定子>
1. 対象は現在も存在するか — 存在するなら該当箇所を引用せよ
2. <当該判定の根拠となる観測> — 引用付きで示せ
判定（件ごと）: 消滅（→リストから除去）/ 確定（→処置実施）/
前回判定が誤り（→内容を報告し処置するな）
```

**形B: 元リストが消失している場合（再検出＋消失の説明責任）**

```markdown
<対象領域> を再検出せよ。
1. 全対象を列挙し（位置付き）、典拠と突合せよ。
2. 異常を列挙し、各件の根拠を引用せよ。
3. 0件と報告する場合の条件: 突合した総数と突合先を示し（全件検分の証拠）、
   かつ「過去に検出された N 件が、いつ・どの変更で解消されたか」を
   git 履歴・差分から説明せよ。説明できない場合、0件報告は認めない —
   検出漏れとみなし、走査方法を変えて再走査せよ。
```

原理: **一度観測された異常は、説明なしには消えない**。ただし——監査が
繰り返し空振りする場合は、**観測装置自身が汚染世界の内側にいないか**
（走査がどのチェックアウトで走っているか）を先に疑う（→T2）。

## T2. 基線の確定と混入監査（develop 軸）（旧#14/14b/14c 統合）

集計の点滅・監査の空振りが続く場合の根治。真因の典型は「ブランチ未指定で
全走査が feature 上で走っていた」— 指定が無ければモデルは現在のチェック
アウトで走る。クリーンな統合ブランチ（develop）を軸にする。

```markdown
1. 前提確認: git diff --stat develop..feature を出力せよ。
   → 人間が確認: 全部お試しなら develop は完全な基線。
     正当な作業が混ざっていればそれだけ develop に取り込んでから次へ。
2. 基線の確定: develop をチェックアウトし HEAD に基線タグを打て。
   scan-scope.md に「走査対象は develop（基線タグ）のツリー。除外ブランチ:
   <お試し実装ブランチ名を名指し>・trial-archive。作業ブランチは基線から
   派生させること」と記せ。git rev-parse HEAD を報告せよ。
3. 混入監査: コード・画面由来の台帳行（items.md source / process-design の
   trigger / meta-sync・ui-binding の evidence / backlog の対象）を develop
   ツリーと突合し、存在しないものを参照する行を「混入行」として列挙
   （削除は承認後）。
```

被害範囲の見立て: 要件由来（REQ・スコープ・決定・DB骨格）はブランチ非依存。
汚染されうるのはコード・画面由来のみ — 作り直しでなく監査と刈り込みで済む。

## T3. 混入行の除去と波及追跡（旧#14d）

```markdown
承認済みの混入行を除去し、波及を追跡せよ:
1. items.md の混入 ITEM を除去した場合 → その ITEM を evidence に持つ
   db-design.yaml の列を列挙せよ（根拠が浮いた列 = 削除候補として人間へ）
2. process-design.yaml の混入 PROC を除去した場合 → その PROC だけが
   reads/writes していた列を列挙せよ（新たに露出した未使用列 = 削除候補）
3. meta-sync / ui-binding / backlog の混入行 → 単純除去でよいが、
   sync 欄から参照が消える行が出たら報告せよ
除去後、3.3 と処理設計のカバレッジ+使用審問を再実行し、前回からの差分のみ
報告せよ。コミットハッシュを含めよ。
```

最終報告で見る3点: 判定の○維持 / 新たに露出した削除候補列 / missing の増加
（増えたら正当行の巻き添え削除を疑う）。除去後は 5.1 再生成で backlog を最新化。

## T4. 台帳の誤削除からの復旧

**複数消えている疑いがあるときは、1件ずつ発見して直さず先に被害の全数確認**
（期待ファイル一覧との棚卸し → git status の D 行 → 一括復元 → 依存順の
再構成 → 全層スイープで完全復旧を数字で宣言）。scenario 等の導出物は
台帳が戻れば再生成できるキャッシュであり、損失としては軽い。
台帳は再生成可能な形で運用しているため、誤削除は3層の安全網で戻せる:
git 履歴 / 導出物からの逆再構成（設計書は台帳の機械生成ビュー、scenario には
reads・writes・rule が焼き込み済み）/ 検証での突合確認。

```markdown
<台帳ファイル> を誤削除した。以下の順で復旧せよ。
1. git から: git log --oneline -- <ファイル> と git status を出力せよ。
   履歴にあれば git checkout <コミット> -- <ファイル> で復元し、
   最終コミット以降に失われた差分が無いか（他の台帳・導出物との食い違い）を
   確認して報告せよ。
2. 履歴に無い場合 — 導出物からの逆再構成: 生成済み設計書を主素材に、
   backlog・scenario 等の関連ファイルを突き合わせて再構成せよ。
   各行に再構成元を注記せよ。
3. 検証: 復旧後、当該台帳に対応する検証（処理台帳ならカバレッジ+使用審問）を
   1回実行し、直近の検証結果の数字と一致するか突合せよ。
   一致=復旧完了 / 差分=それが失われた変更（列挙せよ）。
報告: 復旧経路（git | 逆再構成）/ 失われた差分 <件数> / 検証突合 <一致|差分あり>
```

**複数消失時の全数復旧プロンプト:**

```markdown
誤削除の被害を全数確認してから復旧せよ。1件ずつ発見して直すことを禁ずる。
1. 棚卸し: 以下の期待一覧と現状を突合し、存在 / 消失 の表を出せ:
   requirements.md / items.md / existing-tables.md / db-design.yaml /
   process-design.yaml / meta-sync.yaml / ui-binding.yaml / migration.yaml /
   matrix-spec.md / questions.md / backlog.md / scan-scope.md /
   scenario/ / rehearsal/ / verification/ / docs/design/
2. git を確認: git status（追跡済みの削除は D で見える）と
   git log --oneline -- <消失ファイル> を出力せよ。
   追跡済みの削除分は git checkout で一括復元せよ（最も安い経路）。
3. git に無い消失分だけ、依存順に再構成せよ:
   a. process-design.yaml — docs/design/20-process.md（機械生成ビュー）と
      backlog.md から逆再構成（各行に再構成元を注記）
   b. scenario/*.sql — 復元済みの全台帳から再生成し、SQLite で実行確認
   c. その他も生成元があるものは再生成、無いものは報告
4. 復旧後、整合性スイープ（全層）を1回実行し、直近の全○と数字が一致するか
   突合せよ。一致 = 完全復旧 / 差分 = 失われた変更として列挙。
報告: 消失一覧 / 復旧経路の内訳（git 復元 / 再構成 / 再生成）/ スイープ判定
```

再発防止: 台帳ファイル群はマイルストーン（検証○・差分承認後）ごとに git
コミットする — 誤削除を常に「checkout 1回」の事故にしておく。

**git 管理外の場合の復旧（2026-07-27 実測: 台帳が git の無い場所に生成されて
いた — 安全網の前提が環境と食い違う穴）**: git 経路は使えないので、
実体探索（ごみ箱・.bak・エディタ履歴）→ 導出物からの逆再構成の順。
逆再構成の根拠は使用審問○の実績（全列がどこかの reads/writes に現れる）。

```markdown
db-design.yaml を誤削除した。この場所は git 管理外である — git による復元は
不可能なので試みるな。以下の順で復旧せよ。

0. 実体の探索: ごみ箱・エディタのバックアップ（.bak・自動保存・ローカル
   履歴）・一時フォルダに db-design.yaml の実体が残っていないか探せ。
   見つかればそれを復元し、手順3へ進め。
1. 素材の棚卸し: 以下の存在を確認し、存在するものを列挙せよ:
   docs/design/ の生成済み設計書（10-database.md 等）/ scenario/*.sql /
   process-design.yaml / migration.yaml / meta-sync.yaml / items.md /
   existing-tables.md / speculative 採否・未確定決定の記録 /
   requirements.md（DEC・スコープ宣言）
2. 逆再構成: 存在する素材から db-design.yaml を再構成せよ。優先順:
   - 骨格（テーブル・列・型・必須・store）: 10-database.md があればそれが
     最良（機械生成ビューであり内容は台帳と一致していた）。無ければ
     migration.yaml（store: 独自 の全列を網羅）と process-design.yaml の
     reads/writes・scenario/*.sql から列を全数復元せよ
     （使用審問○の実績により、全列はどこかの reads/writes に現れる）。
   - evidence: 90-traceability.md（REQ→テーブル.列）と items.md から復元。
     復元できない要素は evidence を空欄にせず
     evidence: {lost: 再構成} と注記せよ（後で人間が埋める目印）。
   - 各行に再構成元（どのファイルから復元したか）を注記せよ。
3. 検証: 復旧後、順トレースと削除審問を1回実行し、直近の検証結果の数字
   （missing 0 / 削除提案 0 等）と一致するか突合せよ。
   一致 = 復旧完了 / 差分 = 失われた変更として列挙せよ。
   さらに DDL を SQLite で実行し、scenario/*.sql が全件通ることを確認せよ。

報告: 復旧経路（実体発見 | 逆再構成）/ 素材の棚卸し / 失われた差分 <件数> /
検証突合 <一致 | 差分あり> / DDL・scenario <結果>
```

**再発防止（復旧後に実行）**: 根本原因は誤削除でなく、台帳が安全網の外に
生成されていること。

```markdown
台帳ファイル群（requirements.md / items.md / existing-tables.md /
db-design.yaml / process-design.yaml / meta-sync.yaml / ui-binding.yaml /
migration.yaml / questions.md / backlog.md / scenario/ / docs/design/ 等）が
git 管理外の場所にある。以下を実行せよ:
1. 台帳ディレクトリで git init し、全台帳をコミットせよ（既存リポジトリの
   配下に移動できるならそちらでもよい — どちらが可能か先に報告して人間の
   指示を待て）。
2. 以後、検証○・差分承認のマイルストーンごとにコミットする運用とする。
報告: 選んだ方式 / 初回コミットの対象ファイル数
```

**検証突合「差分あり」の仕分け（逆再構成後）**: 差分の正体は2種 —
失われた変更（正当。再適用する）か、再構成ノイズ（素材の導出物が古い時点で
凍っていた等。除去する）。判別の軸は**記録の有無** — 正当な変更は差分提案の
承認記録・DEC・scenario のどこかに痕跡がある。1件ずつ白黒（集計の食い違いは
集計では解けない、の適用）。

```markdown
復旧した db-design.yaml の検証突合で出た差分を、1件ずつ仕分けよ。
集計だけの報告は不可、行単位で示せ。
各差分（直近の検証結果と食い違う要素）について:
1. 内容: 対象（テーブル.列）/ 差分の向き
   （再構成版に無い | 再構成版にだけある | 属性が違う）
2. 出所の探索: この要素・変更に対応する記録が以下のどれかに残っているか探せ:
   承認済みの DB 差分提案 / requirements.md の DEC / scenario/*.sql /
   process-design.yaml の reads/writes / meta-sync.yaml / 直近の検証報告
3. 判定:
   - 記録あり → 「失われた変更」。記録を典拠に db-design.yaml へ再適用せよ
     （evidence 付き）。
   - 記録なし → 「再構成ノイズ疑い」。勝手に消したり残したりするな —
     `対象 / 内容 / 推奨(残す|消す) / 理由1行` の人間判断リストに出せ。
4. 再適用とノイズ処置（人間承認後）が済んだら、順トレース＋削除審問を
   再実行し、直近の数字との残差を報告せよ。一致したら DDL と
   scenario/*.sql の全件実行まで行え。
報告: 差分 <総数> / 失われた変更（再適用済み）<n> /
ノイズ疑い（人間判断待ち）<n> / 再検証 <一致 | 残差 n> / DDL・scenario <結果>
```

**差分が全件同一カテゴリのとき（実測: 11件全部「属性が違う」）**: 独立した
失われた変更が全部同カテゴリに揃うのは考えにくく、**系統誤差**（再構成時の
変換の癖 — 型の別表記・required の既定の倒し方・store の付け方）を疑う。
かつ属性の事実には正の典拠がある（型・必須=items.md / store=付与規則）ので、
1件ずつの人間判断でなく典拠との機械照合で決着する。

```markdown
「属性が違う」11件を、1件ずつの判断ではなくパターンとして処理せよ。
1. 集計: 11件を `違っている属性 / 値ペア（再構成版の値 vs 突合先の値）` で
   グルーピングせよ。同じ属性・同じ向きの違いは1グループにまとめよ。
2. 属性ごとの正の典拠を宣言し、照合して確定せよ:
   - 型・required → items.md の該当 ITEM が正。両版とも items.md と照合し、
     items.md と一致する側を採用せよ。
   - store → 付与規則が正（既定は「独自」。例外は meta-sync.yaml に対応行が
     ある場合のみ）。規則を適用し直せ。
   - 表記だけの違い（同義の別表記。例: 型名の表記ゆれ）は差分ではない —
     台帳の従来表記に統一し「表記正規化」として数えよ。
   - 上のどの典拠でも決まらない属性のみ、
     `対象 / 属性 / 両方の値 / 推奨 / 理由1行` で人間判断リストに出せ。
3. 全グループの処置後、順トレース＋削除審問を再実行し、直近の数字との
   残差を報告せよ。一致したら DDL と scenario/*.sql の全件実行まで行え。
報告: グループ数と内訳 / 典拠で確定 <n>（items / store規則 / 表記正規化の
内訳）/ 人間判断残 <n> / 再検証 <一致 | 残差 n> / DDL・scenario <結果>
```

**実測の続き: 差分の正体は列属性でなく YAML 上部構造2つ**
（scope.in_scope_requirement_ids 件数 / views 件数）。これらも導出可能 —
in_scope はスコープ宣言から、views は scenario の参照から機械再導出する。

```markdown
差分2グループ（scope.in_scope_requirement_ids の件数 / views の件数）を
導出で決着せよ。どちらの版が正しいかを推測するな — 導出元から計算し直せ。

## scope.in_scope_requirement_ids
1. requirements.md の全 REQ-ID を数えよ。
2. requirements.md 末尾のスコープ宣言から対象外 REQ-ID を数えよ。
3. in_scope = 全 REQ − 対象外 を計算し、ID リストを再生成せよ。
4. 再構成版・突合先の両方とこの導出結果を突き合わせ、導出結果を正として
   db-design.yaml に反映せよ。どちらの版とも一致しない場合は、
   食い違う ID を列挙して報告せよ（スコープ宣言側が変更された可能性 —
   勝手に直すな）。

## views
1. scenario/*.sql の全ファイルを走査し、参照されているビュー名を全数列挙せよ
   （FROM / JOIN に現れる、テーブルでない名前）。
2. 両版の views 定義とこの参照一覧を突き合わせよ:
   - シナリオが参照するのに定義が無いビュー → 「失われた変更」。
     もう一方の版か docs/design から定義を復元して反映せよ。
   - どのシナリオも参照しないビュー → 「過剰候補」。勝手に消すな —
     `ビュー名 / 定義の出所 / 推奨` で人間判断リストに出せ。
3. 反映後の views 件数と、その導出根拠（参照シナリオ名）を表で示せ。

## 仕上げ
順トレース＋削除審問を再実行し、直近の数字との残差を報告せよ。
一致したら DDL（ビュー定義含む）と scenario/*.sql の全件実行まで行え。

報告: in_scope <導出値>（両版との一致関係）/ views <確定件数>
（復元 <n> / 過剰候補 <n>）/ 再検証 <一致 | 残差 n> / DDL・scenario <結果>
```

**処置後も件数が同一のまま動かないとき（実測: 11件が導出決着後も11件）**:
データでなく観測の故障を疑う（監査空振りと同じ型）。仮説3つ —
(a) 編集が別ファイルに当たっている（git 外生成の環境では db-design.yaml が
複数箇所に存在しうる） (b) 突合先が古い導出物で何を直しても同じ差分が返る
(c) 差分リストを再計算せず前回報告を再掲。修正させず観測だけ点検する:

```markdown
差分11件が処置後も同数で残った。修正を試みるな — 観測の点検だけを行え。
1. 突合先の同定: この11件は「何」と「何」の比較か。両側のファイルの
   フルパスと、それぞれがいつ・どうやって作られたもの（原本 | 復旧生成 |
   設計書等の導出物 | 過去の検証報告）かを報告せよ。
2. 実体の数: ファイルシステム全体から db-design.yaml という名前のファイルを
   すべて検索し、フルパス・更新日時・行数を一覧にせよ。2個以上あれば、
   前手順で編集を当てたファイルと、突合が読んでいるファイルがどれかを示せ。
3. 個票1件: 11件から1件だけ選び、以下を現物引用（ファイルパス＋該当行）で
   示せ:
   - 突合の両側の実際の値
   - 前手順で行ったはずの修正が、いま現在ディスク上のファイルに存在するか
     （該当行をそのまま引用せよ）
4. 再計算の証拠: 11件のリストは今この場で再計算したものか、前回の報告の
   再掲か。再計算なら、比較を実行したコマンド・手順を示せ。
報告: 突合先の正体 / db-design.yaml の実体 <個数とパス> /
個票（修正の実在 <有|無>）/ 再計算 <した|再掲>
判定: 差分が残る原因は (a)編集が別ファイル (b)突合先が古い導出物
(c)前回リストの再掲 (d)その他 のどれか — 根拠付きで1つ選べ
```

読み方: 個票で「修正がディスクに実在するのに片側が古い値」→ (a)か(b) /
「修正自体が無い」→ 編集の空振り。(b) なら11件は復旧の失敗ではなく
突合先が古いだけ — 差分あり判定自体が誤警報の可能性。

**(b) 確定時の帰結（実測 2026-07-27）: 完了判定の物差しを差し替える**。
「過去の数字との一致」は突合先ごと信用を失ったので使えない。復旧の完了は
**現在の台帳集合との整合スイープ**で宣言する。教訓: 逆再構成の完了判定を
過去のスナップショットとの一致に置くと、そのスナップショット自体が凍った
導出物である危険を引き込む — 判定は常に生きた台帳との整合で行う。

```markdown
診断の結果、突合先は古い導出物であり「差分11件」は誤警報だった。
以後、その突合先を正として使うことを禁ずる。復旧の完了判定を以下に
差し替える。
1. 現行版の確定: 復旧した db-design.yaml（scope・views は導出決着済み）を
   現行版とせよ。
2. 完了判定は過去の数字との一致でなく、現在の台帳集合との整合で行う。
   以下を順に実行し、スコアカードにまとめよ:
   - DB 層: 順トレース（スコープ宣言の対象外を除く全 REQ が満たされるか）＋
     削除審問（全行検分の表。keep の根拠は REQ|structural|external）
   - 処理層: process-design.yaml との突合 — 全列が reads/writes に現れるか
     （未使用列）、reads/writes の参照先が全て実在するか（幽霊参照）
   - 同期層: meta-sync.yaml の source・field の実在
   - 移行層: store: 独自 の全列が migration.yaml に現れるか
   - 実行層: DDL（ビュー定義含む）を SQLite で実行し、scenario/*.sql を
     全件実行せよ
   報告: `層 / 判定(○|×) / ×の件数とパターン` の表
3. 誤警報の元（古い導出物）の処置: 当該ファイルを特定し、
   - 台帳から再生成できるもの（設計書等）→ 現行台帳から再生成せよ
   - 過去の検証報告 → 「基準として無効」と冒頭に明記しアーカイブへ移せ
報告: スイープのスコアカード / 誤警報元の処置（再生成 <n> | アーカイブ <n>）
判定: 全層○なら復旧完了と宣言してよい
```

**復旧後スイープで×が出たときの閉じ方（実測: DB 層・同期層が×）**:
復旧直後の×の第一容疑は設計の欠陥でなく再構成の残り傷。修正の向きを
**無傷の台帳が正 → 復旧ファイル側を合わせる**に固定する（逆向きは事故を
他の台帳に伝染させる）。DB 層×には evidence: {lost: 再構成} を削除審問が
「根拠なし」と判定した分が混ざる — 削除でなく evidence 復元で解消。

```markdown
スイープの DB 層×・同期層×を解消せよ。db-design.yaml は復旧直後であり、
×の第一容疑は設計の欠陥でなく再構成の残り傷である。無傷の台帳
（meta-sync.yaml・process-design.yaml・items.md・existing-tables.md・
requirements.md）を正とし、db-design.yaml 側を合わせる向きで直せ。
逆向き（無傷の台帳を db-design に合わせて書き換える）を禁ずる。

## DB 層
1. ×の全件を `対象 / 種別(missing | 削除提案) / 内容` で列挙せよ。
2. 削除提案のうち evidence: {lost: 再構成} が原因の件は、削除でなく
   evidence の復元で解消せよ。典拠: 90-traceability.md（REQ→テーブル.列）/
   items.md（ITEM 対応）/ requirements.md の DEC。復元した evidence を
   記入し {lost: 再構成} を消せ。
3. どの典拠からも evidence を復元できない要素のみ
   `対象 / 推奨(削除|残す) / 理由1行` で人間判断リストに出せ。勝手に消すな。
4. missing は該当 REQ の原文を引用し、db-design.yaml のどの要素が欠けて
   いるかを特定して最小差分で補え（再構成の取りこぼし）。典拠から補えない
   件は人間判断リストへ。

## 同期層
1. ×の全件を `field | source / 不在とされた参照先 / 内容` で列挙せよ。
2. 各件について、meta-sync.yaml が参照する名前と db-design.yaml の現状を
   突き合わせよ。db-design 側の列名・テーブル名が再構成で変わっている
   場合、meta-sync の名前が正 — db-design 側を戻せ。
3. 名前の違いで説明できない件（本当に列が無い等）は DB 層の missing と
   同根か確認し、DB 層の解消で消えるか判定せよ。残る件は人間判断リストへ。

## 仕上げ
解消後、DB 層と同期層の検証のみ再実行し（○だった層はやり直すな）、
差分のみ報告せよ。実行層に波及した場合（列名を戻した等）のみ DDL と
該当 scenario を再実行せよ。
報告: DB 層 ×内訳（lost復元 <n> / missing補完 <n> / 人間判断 <n>）/
同期層 ×内訳（名前戻し <n> / DB層同根 <n> / 人間判断 <n>）/
再検証: DB <○|残 n> / 同期 <○|残 n>
```

**ラベルと実質の食い違い（実測: meta-sync に status: draft が残存と報告）**:
検証通過済みの台帳に draft 等の状態ラベルが残っている場合、正は検証記録と
中身であってラベルではない — ただし①実質も未確定の行が混ざっていないか
②直近の作業でラベルが混入していないか、の2点を事実確認してから処置する。

```markdown
meta-sync.yaml の status: draft について、事実確認だけを行え。直すな。
1. 所在: status: draft が現れる箇所を全て引用せよ
   （ファイル冒頭 | 行単位、該当行をそのまま）。
2. 由来: この status 欄は台帳の規約（必須フィールド）に含まれるものか。
   また、今回の復旧作業で meta-sync.yaml に変更を加えたか
   （加えたならその内容を差分で示せ）。
3. 実質の判定: draft とされた各行について、
   - 同期台帳の検証（幽霊・二重の正・sync 整合）を通過済みか
   - source / direction / sync が全て確定値か（未確定・空欄の欄を列挙）
   を判定せよ。
4. 処置の提案（実行するな）:
   - 全欄確定＋検証通過済み → 「ラベルが古い」。status 更新の差分を提案せよ。
   - 実質未確定の欄がある行 → 未確定として questions.md への Q-xx 登録を
     提案せよ。
報告: 所在 / 由来（規約内 | 規約外の残骸 | 今回の混入）/
行ごとの実質（確定 | 未確定欄の列挙）/ 提案（ラベル更新 <n> / Q化 <n>）
```

**Q化承認後の一括処理（駐車＋回答提案まで一気に）**: 5.7 の駐車と 5.8a の
回答提案を1件分に畳んだ複合。人間は○×だけ。

```markdown
meta-sync.yaml の実質未確定1行を、以下の一本道で駐車せよ。
1. questions.md に Q-xx を採番して登録せよ:
   `Q-xx / 内容（何が決まれば確定するか1行）/ 宛先 / 状態: 回答待ち`
2. meta-sync.yaml の該当行の未確定欄に 未確定(Q-xx) と記せ。
   status: draft はこの記法に置き換える — 新しい状態語を発明するな。
3. 波及の逆引き: この行を参照する process（sync 欄の PROC）・scenario・
   backlog 項目を列挙し、回答待ちで駐車すべき実装アイテムがあれば
   backlog.md の状態を「回答待ち(Q-xx)」に更新せよ。
4. 回答提案を1枚作れ（判断は人間がする。根拠なき断定をするな）:
   - 読了証拠: 関係資料（REQ 原文 / DEC / meta-sync 該当行 / mjs の
     読み書き箇所 / existing-tables の usage）から最も関係の深い事実3点を
     出典付きで引用せよ。読了証拠の無い提案は無効。
   - rejected.md に同方向の却下案が無いか照合せよ。
   - 回答案: `Q-xx / 回答案（決定文として使える1〜2行）/ 根拠（出典引用）/
     確度(高|中|低) / 要追認(有|無) / 要件整合（REQ 原文を引用し、原文を
     1文字も変えずに成立することを示せ）`
   - 材料が無ければ「提案不能: 現場でしか分からない理由1行」とせよ。
報告: Q-xx / 駐車した実装アイテム <n> / 回答提案（<内容> | 提案不能）
```

○が付いたら 5.8a の反映手順（本人決定として記録 → DEC 化 → Q 参照の
逆引きで波及解消 → scenario 再生成・実行 → backlog 解禁）で閉じる。
