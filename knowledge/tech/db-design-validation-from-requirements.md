---
type: Consultation
title: 要件定義書からの DB 設計 — 3述語の検証機構と「生成1回＋検証3層」フロー
description: Codex が要件から過剰規模の DB 設計を出す問題。妥当性を「不足なし・過剰なし・実際に回る」の3述語に分解し、根拠タグ付き台帳・削除審問（別セッション adversarial）・DDL 実行検証の3機構と、規模述語をプロンプトに書く予防で解く。続編で3機構をパイプラインに配線 — 生成2段（エンティティ台帳→物理設計）・人間判断点2箇所・fail 時の戻り先定義・失敗のプロンプト還元ループ。
tags: [db-design, codex, verification, traceability, over-engineering, workflow]
timestamp: 2026-07-21T00:00:00+09:00
---

# 相談内容

Codex に要件定義書から DB 設計を作らせると、開発規模が大きくなりすぎる設計が出てくる。
妥当な内容であればよいが、妥当性を検証する方法の用意の仕方がわからない。

# 検討・調査

## なぜ過剰設計が出るか

Codex の失敗ではなく、**規模の述語が欠落した入力への正しい応答**。
「DB設計を出せ」に対する最尤解は、学習コーパス上の"ちゃんとした設計"
（監査列・履歴テーブル・ソフトデリート・マルチテナント・汎用中間テーブル）であり、
規模制約がなければエンタープライズ既定値で埋まる。よって対策は検証機構と入力側予防の両輪。

## 核心: 「妥当か」は判定不能。判定可能な3述語に分解する

> 設計が妥当 ＝ ①不足がない ∧ ②過剰がない ∧ ③実際に回る

1. **不足なし** — 全要件が設計のどこかに対応（要件→テーブル/列の順方向トレース）
2. **過剰なし** — 全設計要素が要件に根拠を持つ（テーブル/列→要件IDの逆方向トレース）。
   根拠を引けない要素＝過剰候補として機械検出できる。今回の悩みの正体はここ。
3. **回る** — 代表ユースケースの SQL が実際に実行できる

[Excel→Web 網羅性検証](/tech/excel-to-web-traceability-verification.md)の「台帳＋契約＋突合」、
[設計書抽出](/tech/design-doc-extraction-from-db-and-code.md)の証拠タグと同じ型の応用。

# 結論

## 機構1: 根拠タグ付き台帳として出力させる

設計を散文・ER図でなく、要素ごとに `evidence: REQ-xx` 必須の台帳形式で出させる
（要件書には事前に ID を振る）。根拠を引けない要素には `speculative: true` を強制。
過剰の検出が読解問題から grep 問題に変わる。

```yaml
- table: order_history
  evidence: []          # 根拠ゼロ → 削除候補として機械抽出可能
  speculative: true
  reason: "将来の監査要件を想定"
```

## 機構2: 削除審問（生成した本人に「妥当か」と聞かない）

生成セッションに自己評価させると自己弁護が返る。**文脈を持たない別セッション**に
要件書と設計だけを渡し、判定の向きを「正当化せよ」でなく「壊せ」にする:

> 各テーブル・各列について削除を試みよ。削除すると満たせなくなる要件IDを挙げよ。
> 挙げられなければ削除を提案せよ。

変形: 最小設計を先に別途作らせて diff を取る。「設計全体の妥当性」という判定不能問題が
「差分N件それぞれに正当化があるか」の Yes/No 列に縮み、人間が判断できるサイズになる。

## 機構3: 実行して検証する

DDL を SQLite/Postgres に実際に流し、要件から起こした代表シナリオ
（画面・帳票・バッチごとの INSERT→SELECT 列）を実行する。
**どのシナリオにも触られないテーブルが過剰の物証**。シナリオ SQL 生成→実行→
未使用テーブル報告まで Codex 自身に実施させられる。

## 予防: プロンプトに規模の述語を書く

- 同時ユーザー数・想定データ行数・運用想定年数・チーム人数を明記する。
- 「履歴テーブル・監査列・ソフトデリート・マルチテナントは要件に明記がない限り禁止」と書く。
  禁止しない限り"ベストプラクティス"として勝手に入る。本当に必要なら Codex が
  「REQ-12 が履歴を要求」と反論してくるので、それ自体が根拠の検証になる。

## 残る主観

正規化の程度などの好みは最後まで主観判断として残るが、機構を通した後は
「根拠タグのない差分を1件ずつ見る」に縮んでいるため判断コストは実用範囲。

---

# 続編（2026-07-21）: 3機構をフローに配線する

「どのようなフローを作るといいか」への回答。原則は
**生成プロンプトを凝るのではなく、検証を機械化してループを回す** —
生成は確率的に外れるが、検証が機械化されていれば外れは自動検出され、
失敗はプロンプトの述語追記に還元できる。

## パイプライン

```
要件定義書
 ↓
[0] 前処理（半自動）: REQ-ID 付与。規模述語（同時ユーザー数・行数・
    運用年数・チーム人数）が要件書に無ければ人間に質問させて埋める
 ↓
[1] 生成①: エンティティ台帳（エンティティ+関連、evidence: REQ-xx 必須）
 ↓
[2] 人間CP①: エンティティ粒度レビュー（10数件なら数分で見られる）
 ↓
[3] 生成②: 物理設計（台帳形式 YAML、全要素 evidence 必須）
 ↓
[4] 機械検証: 双方向トレース突合（スクリプト）
    順方向: 対応ゼロの REQ → 不足 / 逆方向: evidence 空 → 過剰候補
 ↓
[5] 敵対検証: 別セッション削除審問（「壊せ」の向き）
 ↓
[6] 実行検証: DDL 実行 + 代表シナリオ SQL → 未使用テーブル報告
 ↓
[7] 人間CP②: 残った speculative と削除候補だけを Yes/No 判断
```

## 設計判断

- **生成を2段に割る理由は検出**: エンティティ選定の誤りは物理設計に増幅されて
  伝播する。一番安い段（エンティティ10数件）で捕まえる。概念→論理→物理の
  3段フルコースは不要 — AI 相手だと論理段で検出できる失敗クラスが物理段と重なる。
- **人間の判断点は [2][7] の2箇所のみ**: 律速はレビュー容量。機械が判定できる
  もの（トレース突合・DDL 実行）を人間に見せない。[7] に届くころには
  「妥当か」が「この speculative N 件を残すか」の Yes/No 列に縮んでいる。
- **各段に出口条件と fail 時の戻り先を定義**: [4] で evidence 空 → [3] に差し戻し
  「削除か根拠記入か」を選ばせる。[6] の未使用テーブル → [7] の削除候補に積む。
  戻り先がない検証は「報告して終わり」の飾りになる。
- **失敗のプロンプト還元ループ**: [5][7] で削除確定した要素の生成パターン
  （監査列・履歴・ソフトデリート等）を生成①②の禁止述語に1行追記する。
  回すたびに初回生成の的中率が上がる（失敗観測駆動）。

## 実装の器

- ステップ間の受け渡しはすべてファイル（台帳 YAML・突合レポート）。
- [4][6] は決定的なのでスクリプト化。LLM に残すのは生成①②と削除審問だけ
  （[クイズ機構](/tech/knowledge-recall-quiz-design.md)と同じ「決定的部分は機構、
  生成・判定だけ LLM」の分業原理）。
- [5] は文脈を持たない別セッション起動が必須。同一セッションは自己弁護が返る。

## 一発プロンプト版（お試し用・2026-07-21）

パイプラインのうち1プロンプトに畳み込めるもの（規模述語・台帳形式・evidence 強制・
自己トレース検査・禁止リスト・未確定の逃げ道）を全部入れた版。
畳み込めないのは削除審問の独立性（同一セッションは自己弁護）と実行検証の2つで、
前者は「YAML+要件書を新セッションに貼って削除審問2行」が最安のアップグレード。

```markdown
以下の要件定義書から DB 設計を作成せよ。

## 規模の前提（この規模を超える設計は過剰である）
- 同時ユーザー数: <N> / データ行数: 最大テーブルで <N> 行/年
- 運用想定年数: <N> 年 / 開発・運用チーム: <N> 人

## 手順（必ずこの順で、各ステップを見出し付きで出力せよ）
1. 要件抽出: 各要件に REQ-01 形式の ID を振り一覧化。設計に影響しない要件は「対象外」と明記。
2. エンティティ台帳: テーブルでなくエンティティと関連を列挙。evidence: [REQ-xx] 必須。
3. 物理設計: YAML 台帳形式（table / evidence / columns[{name, type, evidence}]）。散文・ER 図不要。
4. 逆トレース自己検査: evidence を直接引けなかった要素を speculative: true で列挙し reason を1行。
5. 順トレース自己検査: どのテーブル・列にも対応しない REQ-ID を列挙（不足ゼロならゼロと明記）。

## 禁止事項（要件に明記がない限り入れない）
履歴テーブル / 監査列 / ソフトデリート / マルチテナント / 汎用中間テーブル / 予約列。
必要と考える場合は根拠となる REQ-ID を挙げて反論すること。

## 出力規律
- 全設計要素に evidence。「一般的なベストプラクティスだから」は evidence にならない。
- 確定できない箇所は勝手に補完せず「未確定: <質問>」として最後にまとめる。
```

試行時の観測ポイント: (a) speculative の件数（ゼロなら自己検査が機能していない疑い）
(b) 禁止事項への REQ-ID 付き反論の有無（来たら正当要件の証拠）
(c) 「未確定」に挙がる項目（要件書側の穴の検出器として機能したか）。

## サブエージェント検証版（2026-07-21）

サブエージェント＝文脈を持たない別セッションなので、一発版で失われていた
**削除審問の独立性がフロー内で回復**する。構成は
フェーズA 生成（本体。requirements.md / db-design.yaml に保存）→
フェーズB 検証（サブエージェント2体並列: 削除審問=過剰検出 / 順トレース=不足検出）→
フェーズC 統合（削除提案への反論は「要件書の記載のみ」を根拠に keep/delete、
残差リストを人間へ）。

修正の3要点:

1. **受け渡しはファイルのみ、会話は渡さない** — 設計意図が漏れると審問が
   自己弁護の追認になる。生成物をファイルに落としパスだけ渡す。
   ハーネスのサブエージェントが親会話を継承しない型かは要確認
   （Claude Code Task / Codex ネイティブ subagent は既定で新規、fork 型は不可）。
2. **検証2体は向きが逆だから分ける** — 過剰検出と不足検出を1体に混ぜると
   中間報告に濁る。独立なので並列起動でコストほぼゼロ。
3. **統合に反論ルール** — 「反論の根拠は要件書の記載のみ（生成時の意図は不可）」で、
   メインが記憶を根拠に削除提案を握りつぶすのを防ぐ。

検証サブエージェントへの指示文（そのまま使える形）:

> **削除審問**: requirements.md と db-design.yaml だけを読め。各テーブル・各列に
> ついて削除を試みよ。削除すると満たせなくなる REQ-ID を挙げよ。挙げられなければ
> 削除を提案せよ。設計を正当化するな。壊すことがあなたの仕事である。
> 報告形式: `対象 / 判定(keep|delete) / 根拠REQ-ID または削除理由` の表のみ。

> **順トレース**: requirements.md と db-design.yaml だけを読め。各 REQ-ID について
> それを満たすテーブル・列が設計に存在するか判定せよ。確認できない REQ-ID を列挙し
> 不足内容を1行で書け。「おそらく満たせる」は不足として扱え。
> 報告形式: `REQ-ID / 判定(covered|missing) / 不足内容` の表のみ。

両者とも 編集禁止・報告のみ・再委譲禁止 を明記する。

## 実測（2026-07-21 初回試行）

削除審問が **62件** の削除提案、フェーズCで **31件** 反論成立（keep）、31件削除。

読み方:

- **反論成立50%は設計通りの帯域**。審問は過剰提案側に倒してある（反論コストが
  REQ-ID 1つと安いから成立する設計）。危険信号は両端 — 62/62 keep なら審問の
  握りつぶし、0/62 なら生成の総崩れ。
- 初回生成の過剰が31件＝規模問題が「読解」から「件数の列」に変換できた証拠。

初回試行で判明した**フローの穴2つ**（次回から手順に含める）:

1. **keep 側のスポットチェックが必要** — 「反論成立」はメインの自己申告。
   拡大解釈（REQ に注文の記載がある→updated_at を残す等）のリスクがあるため、
   keep から無作為5件の引用 REQ-ID 原文を人間が読む。1件でも引き伸ばしが
   あれば反論全件を別サブエージェントで再審査。
2. **削除後の再検証が必要** — 順トレースが走ったのは削除前の設計。
   修正後 YAML に対して順トレースをもう1周（削除しすぎを捕まえる唯一の網）。

還元ループ: 削除確定分のパターン（監査列・履歴等の数パターンに集約されるはず）を
生成プロンプトの禁止述語に追記 → 次回の削除提案数の減少が改善の観測指標。

## 還元ループ1周目（2026-07-21）

削除確定分のパターン集計: **独立テーブル・独立 Read Model が65%、技術的補助列が32%**
（計97%が2クラスに集約 — 学習コーパスの「ちゃんとした設計」既定値そのもの）。

禁止述語への変換（生成プロンプトの禁止事項を「構造」「列」の2階建てに再編）:

- **構造の禁止**: Read Model / CQRS 分離 / 集計・スナップショット・検索用非正規化・
  キャッシュテーブル禁止。読み取りは基本テーブルへのクエリかビューで満たす。
  反論条件は「性能要件（応答時間・行数）の REQ 明記時のみ」と狭く書く —
  曖昧だと「検索を速くしたいはずだから」で復活する。
  FK を持たない独立テーブルは単独で REQ を満たす evidence がない限り禁止。
- **列の禁止**: 表示順・非正規化カウント・導出可能な集計値・キャッシュ列・
  ステータス予備値・汎用 extra/meta カラム。導出できる値は保存しない。
  （履歴・監査列・ソフトデリート等の従来リストは継続）

あわせてフェーズCに「削除後 YAML への順トレース再実行」をプロンプト本体に組み込み。

観測指標: 次回の削除提案数（62 からの減少幅）。特に独立テーブル系がゼロに
近づくか。減らなければ原因は禁止述語の書き方でなく要件書側の曖昧さと切り分けられる。

## 2周目以降の手順と終了条件（2026-07-21）

各周回後: ①4つの数字を取る（削除提案数と独立テーブル系の内訳 / 順トレース再実行の
missing / 残差リスト件数 / keep スポットチェック5件）→ ②分岐:

- 大幅減＋独立テーブル系ほぼゼロ → 還元成功。述語はいじらず残差の人間判断へ。
- ほぼ減らない → 原因は要件書側の曖昧さ。プロンプト強化でなく要件書に明記を足す。
- missing 発生 → 禁止述語が強すぎた可能性。該当項目だけ反論条件を緩める。

**フェーズD: 実行検証**（3述語の「回る」。設計が固まりかけたら同セッション続きで）:
DDL を SQLite に実際に流す → 各画面・帳票・バッチの代表シナリオ SQL
（INSERT→SELECT）実行 → どのシナリオにも触られないテーブル・列を報告（過剰の物証）
→ **シナリオを書こうとして書けなかった要件を報告**（トレース突合で見つからない
不足 — 列はあるが結合できない・状態遷移が表現できない等を捕まえる）。

**終了条件**: (a) 削除提案が反論成立ばかり＝誤検知しか残らない
(b) 順トレース missing 0 (c) 全要件でシナリオ SQL が書けて実行できる — 3つ揃いで
設計確定。確定後は evidence タグ付き db-design.yaml がそのまま設計書、
残差リストの判断記録が ADR 相当になる。

**結果チェック用プロンプト**（実行セッションの続きに貼る。形式のみ・散文不要で
4指標＋機械的判定＋keep 無作為5件の REQ-ID 原文引用を出させる）:

```markdown
今回の実行結果を以下の形式のみで報告せよ。散文は不要。
1. 削除提案: <件数>（前回比）/ 内訳: 独立テーブル・ReadModel系 / 技術的補助列 / その他
2. フェーズC: 反論成立 keep <件数> / 削除確定 <件数>
3. 順トレース再実行: missing <件数>（あれば REQ-ID 列挙）
4. 残差リスト: speculative / 反論keep / 未確定 の各件数
5. 判定: ≦20かつ独立系ほぼゼロ→還元成功 / 前回並み→要件書の曖昧さ(削除理由上位3
   パターン列挙) / missing>0→述語が強すぎ(禁止リストと重なる項目列挙)
6. keep から無作為5件: `要素名 / 根拠REQ-IDの原文引用 / 判定(正当|引き伸ばし)`
```

6は下ごしらえにすぎない — 引用原文5つを人間が読むのがスポットチェックの本体。

## 2周目実測と教訓「逃げ道は生成には効かない」（2026-07-22）

還元1周の効果: 削除提案 **62→18件（71%減）**、狙った2クラス（独立テーブル系・
技術的補助列）は**両方ゼロ**。一方で**順トレース missing 20件**が発生し、内訳
（可変行・セル識別・汎用列 10件 / 外部定量値・Excel計算・しきい値のスナップ
ショット保存 7件）が禁止リストと重なっていた — 過剰を塞いだら不足側に倒れ、
もう1体の検証器が設計通り捕まえた（双方向検証を分けた効果の実証）。
スポットチェックは概ね正当、引き伸ばしは要件書の曖昧表現由来と確定。

**教訓: 禁止事項の逃げ道（REQ-ID を挙げて反論せよ）は審問には効くが、生成は
禁止に素直に服従する**（反論成立3件の裏で missing 20件）。恒久的に必要なものは
「禁止の例外」でなく「明示的な許可」節として禁止事項より優先で書く:

```markdown
### 明示的な許可（禁止事項より優先する）
- REQ-<xx,...> のため、しきい値等の時点保存（スナップショット）は必須。禁止対象外。
- REQ-<zz,...> の可変行・セル識別は <方式> で表現する。
```

処置の3点セット: ①missing を名指しの許可に変換（禁止リスト全体は緩めない —
緩めると62件の世界に戻る）②可変行の表現方式（EAV/JSON列/固定列）は人間が決める
設計判断（[状態モデリング](/tech/state-modeling-for-excel-to-web.md)の領分）で、
決めたら1行でプロンプトに固定 ③未確定6件と曖昧表現は要件書側に明記（プロンプトは
ほぼ完成、残る変動要因は要件書の曖昧さ）。→ 新セッション再走行で missing 0 を
目標にし、達成後フェーズDへ。

## 3周目用統合プロンプトの構成（2026-07-22）

再走行〜フェーズD〜報告を1本に統合（手転記の往復を減らすため）。前版からの差分:

- **「明示的な許可（禁止事項より優先する）」節を禁止事項の前に配置** —
  スナップショット保存の必須化（REQ-ID 名指し）と可変行の表現方式の固定
  （「他の方式を提案しないこと」まで書く）。
- **フェーズCに停止条件**: 統合後の順トレースで missing が 1 件でも残れば
  フェーズDへ進まず停止して報告のみ（不足のある設計で実行検証しても無意味）。
- **フェーズD（DDL + シナリオ SQL）を同一プロンプトに組込み**、missing 0 の場合のみ実行。
- **最終報告を「ID 列挙不要・件数とパターンのみ」の集約形式に固定** + 終了条件
  (a)(b)(c) の ○× 自己判定付き。

実行前の確認2点: 要件書は修正済み新版を貼る（でないと missing 再発の原因が
切り分け不能）/ 許可節の方式プレースホルダは先に人間が決めて埋める
（空欄で走らせるとモデルの事前分布で埋まる）。

## 方式決定: 可変行は固定列に正規化（2026-07-22, Decision）

判別質問3つへの回答: 属性の種類は**有限で列挙できる** / 値は **SQL でしきい値判定に
使う** / セル識別は **Pleasanter 化では不要**。

- **決定: 固定列に正規化**。「列挙可能 × SQL で判定」は固定列の最適条件そのもの
  （型・CHECK・インデックス・普通の WHERE が全部効く）。この条件で EAV は
  しきい値判定が毎回セルフジョインになる純損、JSON 列は列挙できない場合の保険で
  あって列挙できるのに使うと型保証を捨てるだけ。2周目の「可変行・汎用列」は
  **可変に見えて可変ではなかった**。
- **セル識別は不足でなく対象外**。該当 REQ は削除でなく要件抽出で「対象外」と
  明記させる — でないと順トレースが永遠に missing と数える。
- **Pleasanter の汎用項目（分類A〜Z等）と混ぜない**: 設計（論理モデル）は固定列で
  書き、Pleasanter 項目への割り当ては別レイヤーのマッピング表
  （[閉域現場の列マッピング](/tech/closed-network-agent-dev-loop.md)の領分）。
  設計 YAML に ClassA 系の名前が現れたら異臭。

判別質問の一般形（同種の相談で再利用可）: ①属性の種類は設計時に列挙できるか
②値を SQL で検索・集計・比較に使うか ③（位置情報系は）何のために必要か。
列挙可×SQL利用→固定列 / 列挙不能×表示のみ→JSON列 / 運用中にユーザーが属性を
追加×属性単位で検索→そのときだけ EAV。

## 3周目実測 — 後退とフローの構造的な穴2つ（2026-07-22）

結果: 削除提案 **81件**（18→81 に後退、パターン: 親表・技術ID・外部読み取り）、
反論成立44 / missing 18（前回20とほぼ同数）/ speculative 1・未確定 5 /
フェーズDは停止条件どおり未実施。終了条件は全✗。

**故障1: 審問が構造要素を攻撃**。固定列正規化でテーブル・親子関係が増えたが、
主キー・FK・親表は要件書が直接言及しない。審問基準が「REQ-ID を挙げられなければ
削除」だけなので構造必須要素が全部削除候補化。1〜2周目で出なかったのは
わかりやすい過剰が先に狩られていたから — **獲物が尽きた審問は次の獲物に移る**。
穴は審問でなく正当根拠の語彙（構造的必要性が無い）。

**故障2: スコープ判断が検証者に見えていない**。許可節・対象外化はメインの
プロンプトに書いたが、サブエージェントに渡るのは requirements.md と
db-design.yaml のみ。**「独立性のためファイルしか渡さない」ルールが
「ファイルに無い決定は検証者には存在しない」として跳ね返る** — スコープ判断は
ファイルに物質化しない限り検証に反映されない。

4周目パッチ3点: ①requirements.md 末尾に「スコープ宣言」節（対象外 REQ・
時点保存必須 REQ）を生成フェーズで書き込ませる ②evidence 語彙拡張 —
構造要素は `evidence: {structural: <支える要素名>}`、全要素は REQ 直接参照か
structural 連鎖で REQ 持ち要素に到達必須（到達しなければ speculative）。
structural をフリーパスにしない連鎖規則が肝 ③審問に「構造的依存も正当根拠、
structural 宣言は参照先の実在と evidence を検証せよ」、順トレースに
「スコープ宣言の対象外 REQ は除外して報告」を追記。

**missing 18 の正体判明（続報）**: 内容は「細かい項目の定義がないから設計できない」
— フローの故障ではなく**入力の穴の正しい検出**（補完禁止の規律に従った正直な報告）。
「属性は有限で列挙**できる**」（頭・現物 Excel の中にある）ことと
「列挙**されている**」（ファイルにある）ことは別で、固定列方式は列の実名を要求する。

対処: **第3の入力ファイル items.md（項目定義台帳）** を現物から抽出して作る
（ITEM-xx / name / type / required / source / req: [REQ-xx]。
判定できない属性は推測で埋めず「未確定」）。人間の仕事は「未確定」の記入 —
設計判断でなく事実の記入。4周目は「固定列は items.md のみを典拠、無い項目は
設計せず未確定に積む。**サブエージェント2体にも items.md を渡す**」を追記
（ファイル物質化の教訓の適用 — 検証者に見せないと順トレースが言い続ける）。

**抽出元は既存 HTML でよい**（form の name・input type・required 属性は機械的に
拾えるため、列挙目的では Excel より向く）。ただし罠: **画面の項目≠保存する項目**。
表示欄（合計・判定結果）を無邪気に列挙すると「導出データの複製」が items.md 経由で
裏口再入場する。抽出時に `entry: input|display` を必須タグにし、display は一律
「未確定（導出疑い）」マーク（自己判定させない）。人間の仕分け基準は禁止原理の
再利用 — 「再計算できる複製か、その時点の値が業務上の事実か」。複製→列にしない、
事実→保存。型・桁が拾えない項目だけ現物 Excel を補助に。

**第4の入力: existing-tables.md（既存テーブルカタログ）**。missing に「参照予定の
既存テーブルはどれ？」も含まれていた — 同じ病気の3例目（典拠の欠如）。既存 DB /
Pleasanter サイト定義から、参照されうるテーブルの参照情報だけ
（table / key / 主要列のみ / usage 1行。実在確認できたもののみ、推測禁止）を台帳化。
配線: 既存テーブルは設計対象外・変更禁止・参照のみ、参照は
`evidence: {external: <テーブル名>}`、カタログに無いテーブルは参照禁止（未確定に
積む）、審問は external の実在を検証、順トレースは covered(external) を認める。
検証者2体にも渡す。

**一般原理（3つの穴の共通形）**: このフローは「生成と検証の全員が参照してよい
**典拠ファイルの閉じた集合**」を組み上げる作業に収束する — requirements.md
（何を作るか）+ items.md（項目の事実）+ existing-tables.md（既存世界の事実）。
設計はこの集合からのみ導出し、無いものは推測でなく未確定に積む。
missing や「どれ？」が出るたびに典拠ファイルが1枚増えるのは正常動作
（＝要件の穴の検出器が働いている）。新種の「〜が分からない」＝第5の典拠の発見。

## 4周目統合プロンプト v4 の構成（2026-07-22 確定版）

これまでの全修正を1本に統合した版（全文はチャット履歴で提供済み。骨格のみ記録）:

1. **典拠節**（冒頭）: 3典拠ファイルの列挙と「無い事実は未確定に積む」の閉世界宣言。
   items.md は entry:display と未確定を列にしない、existing-tables.md はカタログ外
   参照禁止・変更禁止をここで宣言。
2. フェーズA: 要件抽出 → **スコープ宣言を requirements.md に書込** → エンティティ
   台帳 → 物理設計（evidence 3語彙: REQ+item / structural / external、連鎖規則）。
3. 許可節（禁止より優先）: 時点保存必須 REQ 名指し / 固定列正規化＋EAV・JSON 禁止。
4. 禁止事項: 構造（導出データの複製クラス、反論は性能 REQ のみ）/ 列（補助列ほか）。
5. フェーズB: 検証2体に**4ファイル全部**を渡す。審問は structural/external の
   実在検証込み、順トレースは 対象外/covered(external) を認める報告語彙。
6. フェーズC: 反論根拠は3典拠の記載のみ → 順トレース再実行 → missing 残なら停止。
7. フェーズD: DDL は SQLite、**既存テーブルは key・主要列のスタブ作成可**
   （external FK があるとスタブ無しでは DDL が流れない）→ シナリオ SQL →
   未使用（過剰の物証）＋書けなかった要件（不足の物証）。
8. 最終報告: 件数＋パターンのみの集約形式＋終了条件 (a)(b)(c) の ○×。

実行前チェック4点: プレースホルダ（規模4値・スコープ/許可の REQ-ID）/
items.md の導出疑い仕分け済み / existing-tables.md 配置済み / 新セッション＋新版要件書。

## 4周目実測 — 過剰側の収束（2026-07-22）

削除提案の推移 **62 → 18 → 81 → 0**。evidence 3語彙で正当根拠の語彙が揃い、
狩るものが尽きた。判定 (a) ○。missing 11 / speculative 14 / 未確定 14 が残存、
フェーズDは停止条件どおり未実施。

読み方: **残差は全部「人間待ち」の列** — フローはプロンプト改良フェーズから
「人間が事実と判断を入れるフェーズ」に切り替わった。未確定14は items.md の
未確定を埋める前に走らせた分の可能性が高く、missing 11 も同根の疑い。

健全性チェック: 81→0 の振れ幅なので、審問報告が「全行あり・各 keep に根拠付きの表」
かを確認する（「特に問題なし」の一文なら審問がサボっており再実行）。
**削除提案 0 は「審問が全行を検分した証拠」とセットで初めて信用できる。**

次: ①missing 11 のパターン確認（同根なら入力補充で消える、新種なら第5の典拠）
②items.md 未確定14の記入（事実） ③speculative 14 の Yes/No（判断。採用は
「根拠なし承知で採用」の記録ごと設計の一部）④**プロンプト無変更**で5周目 —
変えるのは入力ファイルだけ。

## 5周目パッチ — 3種類の「無い」の仕分け（2026-07-22）

missing 11 の内訳確認で、性質の違う「無い」が3種と判明。仕分けが次の一手を決める:

1. **「表す構造がない」＝設計判断の放棄（フローの故障）**。閉世界宣言の副作用で、
   生成が事実（型・必須・実在）だけでなく設計判断（項目の配置先・新規テーブル作成）
   まで未確定に逃がした。**推測の禁止が判断の放棄に化ける** — 振り子の3回目。
   対策: 「事実と判断の境界」を明文化（判断は生成の本業、未確定に逃がすな）＋
   **全数配置規則**（型確定済み input 項目は必ずどこかの列に配置）を宣言し、
   順トレースに項目レベルのトレース（ITEM が design に現れるか→missing(item)）を
   追加して機械検証に載せる。
2. **「規則がない」＝当初は業務側の未決定と診断し「保留」カテゴリを提案したが、
   ユーザーの立場（要件は完成している）で差し戻し → 抽出の穴と再診断**。
   「要件は完成」と「規則がない」は両立する — 検証者が見るのは原文でなく
   requirements.md（抽出台帳）であり、**原文→抽出の忠実性はフローの未検証辺**
   だった。規則のような「本文の脇の規範的記述（別表・注記・但し書き・計算式）」は
   抽出（＝縮める操作）で最も落ちやすい。
   対処: ①missing 各件に「原文引用・無かった情報・探した範囲」を報告させる
   特定プロンプト ②人間が原文の該当箇所を指させるか突き合わせ（指させた件＝
   抽出漏れ確定、ID を振って解消） ③v5 に抽出自己検査を追加 — 原文を
   source-requirements.md として保存し、ID 未付与の規則・制約・条件・計算式が
   残っていないか検査。これで原文→抽出→設計→検証の全辺がトレースで繋がる。
   指させない件が残ればそれが「要件完成」への具体的反例リスト —
   保留の棚でなく件ごとに白黒をつける。
   **続報: 「ID は全てに振ってある」→抽出漏れ仮説は棄却**。ID 全付与は
   **網羅性の保証であって充足性の保証ではない**（ID 付きの文に設計可能な粒度の
   規則が書かれているとは限らない）。特定プロンプトの引用を人間が読み、
   「引用に規則がある→モデルの読解失敗（別紙参照・従来通り・準ずる型が落ちやすい。
   該当記述を1行ずつ指せば直る）/ 引用に中身が無い→ID はあるが解像度不足
   （その文だけ設計可能な粒度に書き足す）」の2択で全件に白黒。
   また ID が人間による事前付与なら、フェーズA に **「既存 ID をそのまま使え、
   振り直し禁止」** を必ず追記 — 元指示は「ID を振れ」なのでモデルが自分の
   番号体系で振り直し、突き合わせが壊れる（過去周の番号食い違いの疑いも）。
3. **「型が未定義」＝これから決める事実**。待つのでなく、記入済み実データから
   型を提案させ人間が行ごとに承認する形に変換（ITEM / 提案型 / 実測根拠 / 確度。
   実測根拠なしの提案は禁止）。「決められない」→「承認するだけ」
   （仮説提示型質問と同じ変換）。実測不能の項目だけが人間が頭で決める型。

**v5 最終統合（2026-07-22）**: パッチの小出しでユーザーが迷子になったため、
全差分を1枚に再統合してチャットで提供（構成: 典拠3ファイル+ID振り直し禁止 /
事実と判断の境界 / スコープ宣言 / evidence 3語彙+連鎖 / 全数配置 / 許可・禁止 /
検証2体（ITEM トレース込み）/ 停止条件 / フェーズD / 集約報告 / 個別指示欄
「規則の所在」）。残作業は3ステップ: ①規則5件の引用判定（読解失敗→個別指示欄に
所在を書く / 解像度不足→要件書を書き足す）②プレースホルダ埋め ③新セッション実行。
**運用教訓: パッチは差分で渡し続けると人間側の統合コストで破綻する。
2〜3差分ごとに全文を再統合して「手元に持つのは1枚だけ」に戻すこと。**

**items.md の肥大（entry 分類不能 190件）の対処（2026-07-22）**: HTML から広く
拾うとラベル・見出し・ボタン等の非データ項目が混入する。「input/display を
決められない」こと自体が「項目ではない」のサイン。対処は中身でなく**出所の
HTML 要素で機械仕分け**（入力要素→input / 表示要素→display / label・th・
ボタン・ナビ→削除候補 / 出所不明→削除候補。同名は統合）。
**消しすぎは順トレースが捕まえるので「疑わしきは消す」でよい** — 検出網が
あるから大胆に刈れる（削除審問と同じ理屈が典拠ファイル側に効く）。
再発防止: db-design.yaml の台帳規律を items.md にも適用 — source と req の
両方を書けない項目は台帳に入れない。**典拠ファイル自身も台帳であり同じ
衛生基準に従う**。掃除は5周目より先（全数配置規則がゴミ input を強制配置して
設計が膨れるため）。

**5周目周辺の小トラブル2件（2026-07-22）**: ①items.md 肥大は出所仕分けで解消。
②missing 2件の原因は existing-tables.md の不足 — カタログ外参照禁止が
「既存テーブル名の発明」を missing として可視化した（**カタログの穴も検出器が
捕まえる**）。対処は実在確認のうえカタログに追記し、プロンプト無変更で再走行。

**「existing-tables.md をちゃんと参照しない」問題（2026-07-22）**: カタログを
渡しても missing が減らない。原理: **ファイルを渡すことと読ませることは別** —
典拠に列挙しても消費の保証はない。対策は「読んだ証拠を成果物として強制する」3点:
①フェーズAに既存テーブル対応表（全テーブル×満たしうる REQ-ID、「該当なし」も
明記）を設計開始の前提条件として要求 — 全件について言わせると読み飛ばしが
構造的に不可能になり、省略と判断が区別できる ②順トレースに「既存テーブルで
満たせない理由」列を強制（書けない missing 判定は無効）③カタログに
alias（業務上の呼び名）を追加 — 物理名と要件書の業務語彙の橋。
読んでいても繋がらないケースの保険。
一般形: **渡した典拠は、典拠由来の中間生成物（対応表・理由列）を成果物に
要求して初めて確実に消費される**（プロンプトは命令ではなく証拠、の実践形）。

**DB 設計後の次の成果物（2026-07-23）**: 「設計書を作るべきか」への回答 —
一冊の設計書でなく、**未設計の決定3つを同じ台帳方式で**作る。
①処理設計（本命）: 画面×DB の CRUD 対応表＋計算・判定規則の仕様。
フェーズDのシナリオ SQL がそのまま種になる。process 台帳
（screen / reads / writes / rule / evidence）にすると全 REQ→process（不足）・
全テーブル→process（過剰=未使用検出の正式版）と同じ型で検証が回る。
②Pleasanter マッピング表: 論理列→実装項目（分類A〜Z）。意図的に切り離した宿題。
③移行設計: 既存 Excel→新 DB（required 判定で出た空欄行の処置はここに集約）。
なお db-design.yaml 自体が DB 設計書（evidence 付きで散文より検証可能性が高い）。
人が読む体裁は台帳→文書の一方向で機械生成し、手書きの二重管理を作らない。

**着地点宣言の3値化（2026-07-23）**: store は 独自（既定）/ pleasanter標準 /
既存DB の3値。宣言は事実でなく設計判断なので提案駆動で作る（既定を独自に倒し、
標準行きは「意味一致するメタ情報×標準機能で扱う REQ 根拠」の2条件で挙手制、
既存DB は「内容が既存テーブルに既にある」判定）。**既存DB 分類は二重保存の
検出器を兼ねる** — 該当列は「削除して external 参照に置換」の提案として人間承認へ。
例外: 許可節で必須の時点保存列は既存に元があっても複製でなく業務上の事実
（例外を書かないとスナップショット列が削除候補に戻る）。

**メタ情報ミニマッピング（2026-07-23、確定形）**: メタ情報（状況・担当等）だけ
Pleasanter 標準項目に載せる混在構成と確定。対象＝着地点宣言 store: pleasanter標準
の列のみの閉包（宣言が範囲を定義→マッピングが実装→検証が一致確認）。
**本当のリスクはマッピングでなく二重管理** — メタと業務データが分かれる構成では
①結合キー（独自テーブル行↔ItemId）②両側に同じ事実を持つ列の同期規則
（正はどちら・誰がいつ追従）が未定だと必ずズレる。ミニマッピングの必須
フィールド: map/site/field/type_conv+danger/link/writer/evidence ＋二重管理検出。
検証: 漏れ・越境（独自列の混入）・幽霊項目・link 未定義・writer 整合。
同期未確定は人間の決定→DEC-xx へ。

**マッピング不要への転回（2026-07-23、続報）**: 「拡張HTML を利用、items.md も
拡張HTML の項目から作成」の新情報で前提が崩れた — スロットマッピングの必要性は
「業務データが汎用項目（分類A〜Z）に格納される」前提から来ており、拡張HTML＋
独自 API・独自テーブル構成で着地点が論理設計どおりの実テーブルなら**翻訳層は
存在しない。存在しない層の対応表は儀式** — 作らない。E2E も好転
（scenario/*.sql が実機 DB に直接当たる、辞書不要）。
代わりに最小の1枚「**着地点宣言**」: 各テーブルに store: 独自|pleasanter標準 を
追記（全部独自でも1行宣言する — 「この項目くらい標準でいいか」の場当たり混在を
宣言変更として顕在化させるドリフト防止）。
段2の再定義: 段2-1 は「マッピング」→「責務分界の確定」（impl 仕分けのみ実行。
認証・権限・一覧をどこまで標準に任せるかの線引きが拡張HTML 構成の実装設計の
本体）/ 段2-2 移行設計はそのまま必要。
確認述語: Pleasanter 標準項目に載る業務データがゼロか — ゼロなら宣言+仕分けで
段2完了、あるなら混在部分だけのミニマッピング。
教訓: **成果物の要否はアーキテクチャの事実（データの着地点）で決まる。
前提を確認せずに定石の成果物を要求すると儀式を発注してしまう**。 Pleasanter まるっと新規で
流用先が無くても**作る — 位置づけが「対応付けの記録」から「サイト構築仕様書＋
E2E の辞書」に変わる**。理由: ①論理列→汎用スロットの割当決定はサイト設定の
瞬間に必ず発生する。台帳が無いと決定が設定画面の中で揮発し「分類Cって何だっけ」
ドリフトになる — 選択は「作るか」でなく「決定を記録するか揮発させるか」
②scenario/*.sql は論理名で書かれ実機は汎用スロットに格納するため、E2E の
合否述語（画面操作後の DB 状態一致）はこの辞書無しに原理的に組めない。
新規前提の簡素化: constraints.md は器の種類と上限のみ・サイト設計は自由設計。
追加成果物: mapping.yaml → サイト構築仕様（可能なら Pleasanter サイトパッケージ
JSON）を機械生成し、サイト構築自体を台帳からのインポートにする —
台帳＝実機の一致が構造的に保証される（対応形式は実環境バージョンで要確認）。

**段2-1: Pleasanter マッピングプロンプト（2026-07-23、全文はチャット）**:
対象外218は監査で問題なし→段1完了。段2は「マッピング→移行」の順で2本
（移行はマッピングに依存）。事前典拠 pleasanter-constraints.md
（項目種別と上限・既存サイトの空きスロット。実環境で確認できたもののみ —
existing-tables.md と同じ「器の物質化」）。構成: サイト設計（1テーブル=1サイト
原則）→列マッピング台帳（slot / type_conv+danger / constraint 実現方法）→
容量検査→impl 未定の仕分け → 検証2体（全数突合=漏れ・衝突・danger・実現不能 /
処理整合）。守りどころ: **逃げ道になりうる分類（実現不能・未定・容量超過・
danger）は全部モデルに解消させず人間承認制** —「最安ラベルに流れる」対策の適用。
実現不能制約は検証責務が process（入力チェック）へ移るので処理設計に還流させる。

**処理設計1周目と「対象外の大量発行」（2026-07-23）**: 初回で判定全○だが
対象外 218 件が出た。**「対象外」はカバレッジ検証にとって missing を出さずに
済む最安の逃げ道**であり、検証者が自前判断で大量発行する縮退解の典型。
対象外を決める権限はスコープ宣言（人間）にあり検証者にはない。
監査法: 宣言との突合（宣言外の対象外件数）→ 宣言外は全件 covered|missing に
再判定させる（対象外の再判定は許さない）。妥当なクラス（画面表示のみ等）が
見つかったら人間が宣言に正式追加してから再判定 — 権限の線を守る。
一般形: **検証者に分類語彙を与えるときは、各ラベルの発行権限（機械判定か
人間宣言か）を明示しないと、最も安いラベルに判定が流れる**。

**E2E までの階段（2026-07-23、DB 確定後のロードマップ）**: ゴールを E2E に設定。
段1 処理設計（フェーズD完走=「紙の上の E2E」）→ 段2 実装仕分け
（process ごとに impl: pleasanter標準|独自API|バッチ を割当）＋Pleasanter
マッピング表＋移行設計（検証は全論理列→実装項目の突合）→ 段3 実装と
ローカル E2E（環境は契約だけ複製 — [閉域現場の構成](/tech/closed-network-agent-dev-loop.md)。
**scenario/*.sql の実行結果が E2E テストの期待値に昇格**: 「画面操作後の DB 状態が
シナリオ SQL と一致」が合否述語）→ 段4 E2E 機械化（ブラウザ自動化＋DB 突合 —
[UI 自己検証ループ](/tech/ai-ui-verification-loop.md)の出番）。
設計時の検証資産が実装検証に直結する構造が肝。処理台帳に impl フィールドを
仮置き（未定可）して段2を先取りする。

**完了（2026-07-23）**: フェーズD 0件（DDL 成功・全シナリオ実行可・未使用なし・
書けなかった要件なし）。終了条件 (a)(b)(c) 揃いで **DB 設計確定**。
経過の全体像: 削除提案 62→18→81→0→35→0、missing は入力の穴（項目定義・
既存カタログ・規則解像度）を1枚ずつ典拠ファイル化して解消、最後は対話の
1件ずつ解消＋確定モードで収束。締め3点: 確定版スナップショット（以後の変更は
処理設計からの差分提案経由のみ）/ speculative 採否と未確定決定の記録を yaml の
隣に保存（ADR 相当）/ scenario/*.sql は処理設計の種として保持。

**フェーズD報告の対処規則（2026-07-23）**: 原則「報告は必ずどれかの台帳への
差分に変換する」。①DDL エラー→yaml 側を直して DDL 再生成（DDL 直編集は原本の
二重化）、制約を外して通すのは禁止 ②未使用列→**evidence の強さで振り分け**
（REQ 直接参照あり=シナリオ書き漏れ→シナリオ追加 / speculative・structural のみ
=過剰候補→人間リスト） ③書けなかった要件→3分岐（列不足=DB差分提案 /
関係が表現できない=トレースで見えなかった構造欠陥、個別に見る /
規則が曖昧=未確定リストへ、DB の問題ではない） ④実行できた=合格だが保証は
「回る」まで（値の業務的正しさは別）。シナリオ SQL は scenario/*.sql として
保存 — 処理設計の reads/writes・rule の種になる。

**清書のタイミング（2026-07-23）**: 清書（Markdown 化）は処理設計より**後、最後に
1回だけ**。理由: ①次工程（処理設計）が読むのは yaml であり清書は寄与ゼロ
②処理設計の使用審問が DB 差分提案を出すので DB はまだ動く — 先に清書を作ると
二重管理が始まる（「2つの成果物を同時に動かすと収束しない」の再演）
③清書は成果物でなく**ビュー**（原本は台帳、逆方向編集は原本を2つにする）。
DB の真の確定は「全列がどこかの処理に使われる」の通過時点。
例外: 現場で人に見せる必要があるときだけ途中版ビューを随時生成してよい
（冒頭に自動生成の旨と「修正は yaml 側へ」を明記、yaml に無い情報を足さない）。

**処理設計プロンプトの構成（2026-07-23、全文はチャットで提供）**: 典拠5つ
（requirements.md / db-design.yaml **凍結** / existing-tables.md / items.md /
画面 HTML）→ 処理列挙（PROC-ID・種別・evidence）→ process 台帳
（trigger / reads / writes / rule / evidence、参照は実在するもののみ）→
検証2体（カバレッジ=REQ→process の不足 / 使用審問=全列の used 判定＋幽霊参照検出）
→ 統合 → フェーズD（DDL＋process ごとの SQL 実行が本番の検証）。
DB 設計版との違い3点: ①db-design.yaml は凍結し、穴は「DB 差分提案」として
人間承認後に反映（台帳2つを同時に動かすと収束しない） ②rule は要件原文の
引用主義（「規則がない」再発防止、書けない規則は最初から未確定として表面化）
③フェーズD 到達＝完成（処理設計の正しさは SQL に落ちて実行できるかがほぼ全て）。

**フェーズ移行: 測定モード→確定モード（2026-07-22）**: v6 走行で missing 11→18・
未確定 8→42 と振動。原因はプロンプトでなく**毎回ゼロから再生成していること** —
生成は確率的なので走行ごとに別の設計が出て別の場所に穴が開く。
「新セッションで再生成」はプロンプト効果を測る測定フェーズの規律であり、
削除提案0（4周目）で測定は終わっていた。以後は**確定モード**に切替:
db-design.yaml を凍結し再生成禁止、missing と未確定を典拠から1件ずつ解消する
最小差分のみ許可。解消できない件は「質問/提案/必要な決定」の○×リストに変換。
未確定の相当数は典拠に答えが既にある消費失敗の再発と推定 → items.md との
全件照合を義務化。以後の周回は「解消済みn・残m」の単調減少になり、
増えたら差分モード違反のシグナル。
**一般形: 測定フェーズ（新セッション・全再生成・1変数変更）と確定フェーズ
（成果物凍結・差分解消・アンカー歓迎）は目的が逆なので規律も逆になる。
測定の規律を確定フェーズに持ち込むと収束しない。**

**required の未確定の対処（2026-07-22）**: required は2つの意味の混合 —
①DB の NOT NULL（過去の全データ含め空が無いことを要求）②画面の必須入力
（新規入力で空を許さない）。DB 設計で決めるのは①だけで、①には動かぬ証拠
＝**現行実データの空欄率**がある。判定規則: 空欄>0 → false 確定（現行業務が
空欄で回っている実績＋移行時に NOT NULL が成立しない）/ 空欄=0 → true 提案
（観測件数で確度、人間が○×）/ データに現れない → 実測不能のみ人間判断。
false を覆して必須にしたい場合は「既存空欄行の移行時の扱い（補完/破棄）」の
意思決定とセットでのみ可 — しないなら DB は NULL 許容で確定し、
「必須にしたい」は画面側の必須チェック（DB 設計の外）として2層に分離する。
型と同じ「決められない→実測に○×するだけ」変換。

走行順: 型提案→承認→items.md 更新 / 規則系 REQ を保留宣言 / パッチ入り v5 で5周目。

## 構造禁止リストの原理（2026-07-22, 「何の懸念か」への回答）

Read Model・CQRS・集計・スナップショット・検索用非正規化・キャッシュの共通原理は
**導出データの複製**（基本テーブルから計算できるものの二重保存）。懸念は3段:
①複製を持った瞬間に同期という仕事（更新経路+テスト+不整合対応）が付いてくる
②対価は性能問題が実在するときだけで、小規模ではクエリ/ビューが同期コストゼロで
同じ要件を満たす（早すぎる最適化のスキーマ焼き付き）③LLM の事前分布の定番なので
禁止しないと要件と無関係に混入する（実測: 初回削除確定の65%、禁止後ゼロ）。
反論条件を性能要件明記のみに絞るのは、このクラスの存在理由が性能しかないから。
しきい値の時点保存が許可節で除外されるのは「導出の複製」でなく
「その時点の値が業務上の事実（再計算不可）」であり原理の適用外だから。
